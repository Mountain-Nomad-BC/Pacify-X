# Existing project workspace prompt

You are operating the Engineering Loop Bootstrap at `<BOOTSTRAP_DIR>`. The governed multi-project workspace is `<WORKSPACE_DIR>`. The user has placed the existing repository at `<WORKSPACE_DIR>/projects/<PROJECT_NAME>`.

Preserve the repository's architecture, instructions, source, settings, build/test systems, CI, security controls, and ownership. Begin read-only. Never move an external repository into the drop root without explicit user authorization, and never overwrite an existing owner file.

1. Resolve the bootstrap, workspace, and dropped repository paths. Reject symlink/path escape, nested project ambiguity, or a repository outside the direct `projects/<PROJECT_NAME>` drop location.
2. Validate the framework with `engineering-bootstrap doctor` and `engineering-bootstrap validate`. If needed, use `python -m runtime.cli --root "<BOOTSTRAP_DIR>" ...` from the source checkout.
3. Preview `engineering-bootstrap workspace init --workspace "<WORKSPACE_DIR>"`; stop for approval before applying it with `--apply` when the workspace is new.
4. Run `engineering-bootstrap intake --project "<WORKSPACE_DIR>/projects/<PROJECT_NAME>"` and treat documentation claims as hypotheses until code/runtime evidence supports them.
5. Preview `engineering-bootstrap workspace discover --workspace "<WORKSPACE_DIR>"`. Review every proposed project identity and commissioning mode. Stop for approval, then apply with `--apply`.
6. Confirm differing owner files were preserved byte-for-byte. Run `engineering-bootstrap workspace status --workspace "<WORKSPACE_DIR>"` and require a valid central registry, local workspace binding, isolated tracking directory, and isolated memory root.
7. Allocate a stable `<AGENT_ID>` and `<SESSION_ID>` for this agent/team. Activate the registered project using `engineering-bootstrap project activate --workspace "<WORKSPACE_DIR>" --project-id "prj_<PROJECT_NAME>" --agent-id "<AGENT_ID>" --session-id "<SESSION_ID>"`. One session may hold only one writable project; separate sessions may operate separate projects concurrently. Read `projects_tracking/PROJECT_MANAGEMENT.md`, the project's `PROJECT_MANAGEMENT.md`, its stored inventory, adoption plan, and machine-readable state.
8. Run `engineering-bootstrap project-check --project "<WORKSPACE_DIR>/projects/<PROJECT_NAME>"` and `engineering-bootstrap startup --project "<WORKSPACE_DIR>/projects/<PROJECT_NAME>"`. Startup must report zero hydrated skill bodies. Review its read-only tooling assessment against the repository's actual stack, separate observations from recommendations, and present any proposed installation/configuration effects for explicit approval; never install from the startup result itself. If a standalone report is useful, run `engineering-bootstrap tooling assess --project "<WORKSPACE_DIR>/projects/<PROJECT_NAME>"`.
9. Copy `bootstrap/commissioning/questionnaire-template.json`, set `mode` to `existing`, complete it from verified repository evidence and user decisions, preview `engineering-bootstrap brief --project "<WORKSPACE_DIR>/projects/<PROJECT_NAME>" --questionnaire <answers.json>`, and apply it only after approval with `--apply`. Then run `engineering-bootstrap lifecycle status --project <PROJECT_DIR>` and follow its next-stage contract.
10. Use `working-set`, then run `engineering-bootstrap hydrate --skill <selected-id>` for one bounded capability at a time. Use `workflow list` and previewed `workflow run` requests for registered orchestration. Require declared effects, explicit approval, idempotency, checkpoints, and persisted receipts.
11. Memory sources must be files inside the active project. Use `memory ingest`, evidence-backed `memory transition`, `memory correct`, `memory search`, `memory maintain`, and `memory status`, always passing the same `<AGENT_ID>` and `<SESSION_ID>`; never point the CLI at another project's memory root. A correction must not suppress certified memory until the correction is certified.
12. To change projects within the same session, checkpoint current work and run `project activate` for the destination with `--session-id "<SESSION_ID>" --context-reset-confirmed`. The switch must revoke the old lease, rebind roots, and pass the negative old-project access test.
13. Release the final lease with `engineering-bootstrap project release --workspace "<WORKSPACE_DIR>" --session-id "<SESSION_ID>" --context-reset-confirmed`.

Stop closed on project-root ambiguity, unexpected mutation, secret exposure, owner collision without preservation, registry or binding drift, missing approval, foreign context, cross-project memory, or missing completion evidence.
