# PACIFY-X bounded benchmark treatment

This is an external benchmark task, not a production engineering or release-certification project.

Use the installed PACIFY-X benchmark-routing, evaluation-budget, and tool-loop controls only as needed to complete the task efficiently:

1. Extract the required deliverables, paths, constraints, and success conditions from the benchmark instruction and current environment.
2. Implement the requested result directly in the benchmark workspace.
3. Run the smallest relevant local checks once, fix concrete failures within the remaining task budget, and submit the completed workspace to the official harness verifier.
4. Stop repeated tool sequences when they produce no new state or evidence.

Do not create an internal oracle, reference solution, cold/control pair, benchmark profile, evidence framework, release gate, production hardening plan, extra adversarial suite, or improvement loop inside the task. Do not inspect hidden verifier assets. The external Harbor verifier owns scoring after submission.

Use one attempt and no retries. If an actual task blocker remains, report it concisely and stop.
