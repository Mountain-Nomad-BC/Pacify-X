# Trust-boundary hardening punch-card ledger

All nine cards are integrated in one coherent repair commit, `4d795e11210d076c632e8135470ed273a9ece872`. Common generated projections were rebuilt after the implementation stabilized: contract ownership, Python surfaces and dependencies, effect surfaces, reachability, graphs, semantic capability index, commissioned skill registry, and registry envelope inventory.

| Card | Status and acceptance | Primary changed surfaces | Existing systems reused | Tests and negative evidence | Limitation |
|---|---|---|---|---|---|
| PC-TB-001 | Passed. Caller claims cannot produce authoritative verification. | `runtime/trusted_evidence.py`, `runtime/outcome_verifier.py`, outcome/trusted-evidence schemas, trust docs | Existing OpenSSH/Ed25519 canonical signing and signer policy | `test_trust_boundaries.py`, `test_outcome_verifier.py`; missing, tampered, stale, cross-scope, and unapproved-producer cases | Trusted producers still require operator provisioning. |
| PC-CLI-002 | Passed. Admission dispositions have stable non-success exits. | `runtime/exit_codes.py`, `runtime/cli.py`, CLI exit-code contract | Existing admission state machine and JSON result model | `test_cli_commands.py`, `test_admission_controller.py`; restrict, quarantine, reject, malformed-input paths | Automation relying on the old ambiguous zero exit must migrate. |
| PC-SEC-003 | Passed. `authorize` requires trusted policy evidence; simulation is explicit. | `runtime/execution_contract.py`, `runtime/effect_grants.py`, authorization schema | Existing execution envelope, effect grants, identity/scope controls | `test_trust_boundaries.py`, `test_effect_grants.py`; scope, expiry, signature, replay/idempotency, and non-read denial | Operational writes still require a separately provisioned effect grant. |
| PC-ADM-004 | Passed. Admission facts derive from signed receipts. | `runtime/admission_controller.py`, candidate-admission schema, admission skill docs | Existing deterministic admission classifier and shared resolver from PC-TB-001 | `test_trust_boundaries.py`, `test_admission_controller.py`; forged Boolean and incomplete-evidence cases | Receipt producers remain separate from the admission decision engine. |
| PC-ENV-005 | Passed. Runtime, packaging, policy, and CI agree on Python 3.11-3.14. | `runtime/platform_support.py`, doctor, dependency audit, CI, `pyproject.toml` | Existing `policies/platform-support.json` as authority | `test_platform_support.py`, `test_dependency_audit.py`; 3.10 and 3.15 rejected | A future Python minor requires an intentional policy and matrix update. |
| PC-REL-006 | Passed. First-run onboarding uses immutable v0.6.3; development install is separate. | `README.md`, `START_HERE_FOR_AI.md`, `docs/release-process.md`, `SECURITY.md` | Existing release verifier and immutable signed tag | `test_public_repository_surface.py`, installed-wheel smoke; tamper behavior remains in release tests | The new post-0.6.3 changes require a future signed release before certification. |
| PC-EVD-007 | Passed locally. Complete evidence is deterministic, chunked, content-addressed, reconstructable, and signed by the release workflow. | `runtime/evidence_custody.py`, `scripts/package_release_evidence.py`, release workflow | Existing release signing and GitHub Release publication | `test_evidence_custody.py`; missing and corrupted chunks fail closed | Publication occurs only during an authorized future signed release. |
| PC-ARC-008 | Passed. Basic commands import only their selected command family. | `runtime/cli.py`, installed-resource path handling | Existing deterministic parser and package-data resolver | `test_cli_lazy_loading.py`, installed-wheel E2E; unrelated import-failure isolation | Parser construction remains centralized for deterministic help compatibility. |
| PC-CI-009 | Passed. Normal and scheduled CI consume the hash lock; assurance gates are independently receipted. | `runtime/gate_runner.py`, CI workflows, dependency audit, gate docs | Existing lock file and seven canonical validators | `test_dependency_audit.py`, `test_public_repository_surface.py`, `test_independent_gate_runner.py`; stale/tampered/failed receipt cases | GitHub publication and OS/Python matrix execution remain external CI actions. |

No competing verifier, policy engine, registry, or `v2` implementation was introduced. Compatibility command changes are documented in `docs/trust-boundary-migration.md`.
