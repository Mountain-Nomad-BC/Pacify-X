interface:
  display_name: "AI Runtime Assurance"
  short_description: "Audit opted-in redacted AI runtime telemetry"
  default_prompt: "Use $audit-ai-runtime-assurance to produce a privacy-bounded runtime passport, drift assessment, and certification state."
