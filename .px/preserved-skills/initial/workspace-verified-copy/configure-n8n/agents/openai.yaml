interface:
  display_name: "Configure N8N"
  short_description: "Configure n8n URLs, database, encryption, execution, logging, retention, and run"
  default_prompt: "Use $configure-n8n to configure n8n URLs, database, encryption, execution, logging, retention, and runtime settings from an explicit contract."
