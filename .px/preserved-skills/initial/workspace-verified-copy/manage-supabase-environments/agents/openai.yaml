interface:
  display_name: "Manage Supabase Environments"
  short_description: "Isolate local, preview, staging, and production Supabase projects, credentials, "
  default_prompt: "Use $manage-supabase-environments to isolate local, preview, staging, and production Supabase projects, credentials, links, secrets, and promotion paths."
