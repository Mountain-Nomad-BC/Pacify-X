interface:
  display_name: "Connect N8N To Supabase"
  short_description: "Connect n8n to Supabase through the built-in node, Data API, RPC, or Postgres wi"
  default_prompt: "Use $connect-n8n-to-supabase to connect n8n to Supabase through the built-in node, Data API, RPC, or Postgres with correct URLs, schemas, credentials, and RLS semantics."
