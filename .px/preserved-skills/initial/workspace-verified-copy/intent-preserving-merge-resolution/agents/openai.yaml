interface:
  display_name: "Intent Preserving Merge Resolution"
  short_description: "Resolve merge and rebase conflicts by reconstructing each side\u2019s intent and pres"
  default_prompt: "Use $intent-preserving-merge-resolution to resolve merge and rebase conflicts by reconstructing each side\u2019s intent and preserving both valid outcomes."
