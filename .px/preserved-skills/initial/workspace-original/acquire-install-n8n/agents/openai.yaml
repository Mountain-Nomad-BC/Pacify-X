interface:
  display_name: "Acquire Install N8N"
  short_description: "Acquire and install n8n through a supported, pinned, reversible channel."
  default_prompt: "Use $acquire-install-n8n to acquire and install n8n through a supported, pinned, reversible channel."
