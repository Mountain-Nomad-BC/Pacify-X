interface:
  display_name: "Build Supabase Edge Functions"
  short_description: "Build Supabase Edge Functions with explicit auth, secrets, CORS, bounds, idempot"
  default_prompt: "Use $build-supabase-edge-functions to build Supabase Edge Functions with explicit auth, secrets, CORS, bounds, idempotency, tests, and deployment evidence."
