interface:
  display_name: "Manage Agent Session Fabric"
  short_description: "Plan authority-separated terminal session actions"
  default_prompt: "Use $manage-agent-session-fabric to validate this optional project-scoped terminal session action without executing it."
