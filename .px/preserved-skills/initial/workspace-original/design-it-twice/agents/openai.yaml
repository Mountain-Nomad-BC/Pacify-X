interface:
  display_name: "Design It Twice"
  short_description: "Generate and compare genuinely different interface designs before locking an imp"
  default_prompt: "Use $design-it-twice to generate and compare genuinely different interface designs before locking an important seam."
