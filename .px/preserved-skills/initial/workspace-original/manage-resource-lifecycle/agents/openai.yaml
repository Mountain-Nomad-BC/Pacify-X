interface:
  display_name: "Manage Resource Lifecycle"
  short_description: "Reconcile owned resources without unsafe deletion"
  default_prompt: "Use $manage-resource-lifecycle to classify, retain, reconcile, and safely reclaim PACIFY-X-owned resources."
