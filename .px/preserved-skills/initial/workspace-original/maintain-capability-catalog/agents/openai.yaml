interface:
  display_name: "Maintain Capability Catalog"
  short_description: "Reconcile capability metadata and ownership"
  default_prompt: "Use $maintain-capability-catalog to reconcile capability metadata, ownership, lifecycle, and discoverability without eager hydration."
