interface:
  display_name: "Govern Cross-Harness Runtime"
  short_description: "Normalize runtime state across agent harnesses"
  default_prompt: "Use $govern-cross-harness-runtime to normalize and validate cross-harness state without widening authority."
