interface:
  display_name: "Evaluate Supabase Fit"
  short_description: "Evaluate Supabase against direct Postgres, another backend platform, or custom s"
  default_prompt: "Use $evaluate-supabase-fit to evaluate Supabase against direct Postgres, another backend platform, or custom services."
