interface:
  display_name: "Acquire Install Supabase"
  short_description: "Acquire and install the Supabase CLI and official agent resources using supporte"
  default_prompt: "Use $acquire-install-supabase to acquire and install the Supabase CLI and official agent resources using supported, pinned channels."
