{
  "authoritative_source_sha256": "59e94c1713d8a8b8ac292887dd136c885f3c4ee1f942d20b2256b8b19b68362e",
  "authority_and_safety": "Default to read-only analysis; never widen a patch or infer ownership without traceable repository evidence.",
  "evidence": [
    "normalized inputs",
    "owner and dependency resolution",
    "structured result",
    "negative-case result",
    "content hashes",
    "verification receipt"
  ],
  "failure_policy": "Fail closed on missing scope, authority, consumer, oracle, rollback, or evidence; preserve the last valid state and report the exact blocker.",
  "id": "patch-scope-guard",
  "implementation_disposition": "namespaced_exact_body",
  "implementation_target": ".px/skills/analyze-repository-intelligence/scripts/authoritative_patch_scope_guard.py",
  "inputs": {
    "constraints": "Authority, time, cost, privacy, compatibility, and mutation limits.",
    "evidence_context": "Known facts, source hashes, prior receipts, and unresolved contradictions.",
    "target": "Explicit file, repository, service, record set, or artifact scope."
  },
  "kind": "script",
  "procedure": [
    "inventory-repository",
    "trace-relationships",
    "form-testable-hypotheses",
    "measure-change-impact",
    "propose-minimal-safe-action"
  ],
  "recovery": "Resume from the last hash-bound checkpoint after revalidating inputs and affected dependencies; quarantine suspect generated artifacts instead of deleting them.",
  "source_paths": [
    "packs/02-repository-intelligence-aci_v1.0.0/scripts/patch_scope_guard.py"
  ],
  "trigger": "Use when the requested outcome is patch scope guard or when that outcome is a required dependency."
}
