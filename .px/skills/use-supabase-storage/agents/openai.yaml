interface:
  display_name: "Use Supabase Storage"
  short_description: "Design Supabase Storage buckets, paths, uploads, signed access, transformations,"
  default_prompt: "Use $use-supabase-storage to design Supabase Storage buckets, paths, uploads, signed access, transformations, and RLS policies."
