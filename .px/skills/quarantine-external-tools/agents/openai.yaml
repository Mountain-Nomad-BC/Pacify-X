interface:
  display_name: "External Tool Quarantine"
  short_description: "Gate packages and tools before execution"
  default_prompt: "Use $quarantine-external-tools to assess a package, extension, MCP, binary, or container before integration."
