interface:
  display_name: "Manage Supabase Migrations"
  short_description: "Create, review, test, deploy, reconcile, and roll back Supabase migrations witho"
  default_prompt: "Use $manage-supabase-migrations to create, review, test, deploy, reconcile, and roll back Supabase migrations without dashboard drift."
