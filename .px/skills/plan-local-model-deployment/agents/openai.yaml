interface:
  display_name: "Plan Local Model Deployment"
  short_description: "Plan feasible local model execution strategies"
  default_prompt: "Use $plan-local-model-deployment to rank safe local execution strategies for this model, workload, and hardware."
