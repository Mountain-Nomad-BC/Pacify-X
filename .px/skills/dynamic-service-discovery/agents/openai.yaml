interface:
  display_name: "Dynamic Service Discovery"
  short_description: "Reject stale cached network identity"
  default_prompt: "Use $dynamic-service-discovery to perform this workflow with explicit evidence and safety boundaries."
