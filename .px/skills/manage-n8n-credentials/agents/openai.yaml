interface:
  display_name: "Manage N8N Credentials"
  short_description: "Create, scope, rotate, test, and retire n8n credentials without exposing values "
  default_prompt: "Use $manage-n8n-credentials to create, scope, rotate, test, and retire n8n credentials without exposing values or breaking hidden consumers."
