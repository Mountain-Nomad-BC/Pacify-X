# Result-aware validation

- Assert user-visible states, errors, accessibility semantics, and backend result contracts—not clicks alone.
- Gate operations on required data freshness, completeness, ownership, and schema compatibility.
- Detect ambient writes by snapshotting storage, filesystem, process, and service state around read-only paths.
- Include negative tests for empty, stale, unauthorized, partial, and contradictory results.
