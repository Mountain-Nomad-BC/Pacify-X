interface:
  display_name: "Scale N8N Queue Mode"
  short_description: "Design and operate n8n queue mode with Postgres, Redis, workers, webhook process"
  default_prompt: "Use $scale-n8n-queue-mode to design and operate n8n queue mode with Postgres, Redis, workers, webhook processors, and task runners."
