{
  "gap_id": "PX-OS-079",
  "note": "After the three fixture corrections, the governed dashboard-extension walk advanced to a newly exposed interaction failure. A skill save conflict can refresh the editor and leave focus outside #app; Escape is registered only on #app, so the visible modal does not close. The subsequent Stage skill candidate click is intercepted by the still-open editor. studio-memory-graph was not run.",
  "patch": {
    "next_action": "Make Escape dismissal document-scoped while a dialog is visible, preserving graph-focus handling and Studio trust cleanup; then rerun only dashboard-extension and run studio-memory-graph only if it passes. Do not run the full profile."
  },
  "evidence": [
    {
      "reference": ".engineering-bootstrap/test-evidence/sections/dashboard-extension.json",
      "claim": "Failed governed section receipt for the newly exposed modal interaction failure"
    },
    {
      "reference": "extension/tests/dashboard-e2e.test.js:304",
      "claim": "The operational walk sends a failed retry response, presses Escape, and then attempts a fresh skill draft"
    },
    {
      "reference": "extension/media/dashboard/90-controller.js:2028",
      "claim": "Keyboard handling is scoped to #app, so Escape is lost when refreshed modal focus falls back outside #app"
    }
  ]
}
