---
canonical_id: "mcproutes"
kind: system
layer: host
currentness: same-file-bytes
runtime_observed: false
certified: false
---
# Concrete MCP operation dispatch

[[Layers/host]] · [[Views/Full_Architecture]]

## Purpose

Binds strict tool schemas to bounded dashboard subprocess queries or direct JavaScript coordination, enterprise and team-package operations.

## Historical source state

Tool inputs, authority-wrapped invocation, JSON result and activity events.

## Limits and unknowns

This inspected server offers context/catalog/coordination operations; its task-plan and claim handlers do not launch an arbitrary agent objective.

## Historical suggested evolution

No autonomous adaptation established; changes require its declared caller or owner.

## Evidence scope

Historical source model; file hashes refreshed. Byte agreement does not prove current behavior. This note is not a repair-closure receipt.

- [[Evidence/S858]] — same-file-bytes
- [[Evidence/S859]] — same-file-bytes
- [[Evidence/S849]] — same-file-bytes

## Directed relationships

- [[Systems/taskdag]] — dispatches parallel plan creation (`E214`)
- [[Systems/tasklease]] — dispatches claim renewal and progress (`E215`)
- [[Systems/taskreconcile]] — dispatches reconciliation and release (`E216`)
- [[Systems/teampack]] — re-inventories and stages selected candidates (`E220`)
- [[Systems/mcpbundle]] — supplies build entry source (`E329`)
- [[Systems/enterprisecontrol]] — initializes enterprise state on two read-only routes (`E331`)
- [[Systems/mcp]] — wraps annotation-selected authority and completion events (`E333`)
- [[Systems/coordread]] — reads coordination through multiple nominally read-only tools (`E363`)
- [[Systems/coordresume]] — returns stored packet without checking its own seal (`E367`)
- [[Systems/envread]] — reads retained environment subjects and extension details (`E374`)
- [[Systems/pysnapshot]] — uses composed snapshot for summary hardware readiness and enterprise catalog (`E390`)
- [[Systems/graphread]] — queries normalized stored graph directly (`E397`)
