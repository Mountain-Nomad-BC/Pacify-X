# Excluded entry budget is checked after each directory is materialized

**Historical finding; current closure not revalidated.**

Excluded scanner sorts the entire scandir iterator before incrementing visited_entries and applying its million-entry bound. It limits processed entries but does not cap a single directory listing allocation. Its metadata methods can race directory replacement; no stable physical handle or second tree equality is retained. 

- [[Evidence/S028]]
