# Artifact binding requires presence rather than exactly one of each type

**Historical finding; current closure not revalidated.**

Binding uses next wheel and next sdist; despite error wording it does not reject additional same-type records with different names. All supplied records receive byte checks, but only first wheel/sdist receive version/projection inspection.

- [[Evidence/S2899]]
