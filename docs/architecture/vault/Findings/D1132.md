# Byte stability test uses check mode only

**Historical finding; current closure not revalidated.**

Generated-artifact test snapshots templates before/after check=True calls, establishing check-mode nonmutation when current. It does not exercise actual generation followed by byte-identical regeneration. Separate wrapper fixture does detect one injected drift.

- [[Evidence/S4212]]
