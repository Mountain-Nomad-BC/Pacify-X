# Root grouping requires exact whole signatures and caller denominator

**Historical finding; current closure not revalidated.**

Groups equal evidence/owner/dependency tuples; partially overlapping causes stay separate. Expected duplicates collapse to set and empty IDs are not explicitly rejected. Candidate sequential prefix changes when earlier groups are introduced.

- [[Evidence/S2956]]
