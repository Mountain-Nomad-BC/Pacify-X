# Late-card framed hash and rollback checks cover distinct moments

**Historical finding; current closure not revalidated.**

Independent hash is computed after rollback against the earlier promotion hash, alongside exact first-projection byte restoration. Preserved provenance is checked at draft/validation/admission/promotion/projection stages. Authenticated committed transaction is verified for second promotion; rollback_projection_authenticated itself compares path sets, with receipt authentication owned elsewhere.

- [[Evidence/S1420]]
