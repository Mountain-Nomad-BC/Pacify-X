# Agent structural admission can coexist with blocked runtime memory

**Historical finding; current closure not revalidated.**

The unresolved-memory fixture passes candidate tests and admission but marks runtime_ready false; preview blocks and invocation must fail before adding a run. Host tool preflight likewise tests structural attestation and authority preview, not an actual external host tool invocation.

- [[Evidence/S4088]]
