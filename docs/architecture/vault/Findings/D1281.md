# Discovery identity and requirements can drift across inventory revisions

**Historical finding; current closure not revalidated.**

Missing-card allocation uses a sampled maximum ID outside the append lock and existing cards are indexed by control_action without surface identity. Initial feature requirements pin the discovery inventory hash; later synthesis can retain that old requirement revision with newer aggregate evidence.

- [[Evidence/S3938]]
