# Configuration fault once markers differ from host-action markers

**Historical finding; current closure not revalidated.**

Configuration fault injection only checks marker existence and does not consume it, so a .once filename can repeatedly fail until external removal. Host-action fault injection checks plain-file JSON schema/operation/effect, unlinks then throws. Both routes are gated by owned-host flags but implement different failure lifetimes.

- [[Evidence/S1076]]
