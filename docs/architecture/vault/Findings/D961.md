# Session parity can pass while authority differs

**Historical finding; current closure not revalidated.**

Comparator valid is based on selected semantic fields and excludes authority equality. It separately returns authority_equal, so consumers must inspect both fields. Empty or weakly shaped mappings can compare equal without normalized session provenance.

- [[Evidence/S2164]]
