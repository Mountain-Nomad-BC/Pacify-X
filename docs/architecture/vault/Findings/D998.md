# Helper source hash precedes a separate execution open

**Historical finding; current closure not revalidated.**

Each registry target hash is checked once before subprocess runs or cache reuse. No immutable copy/handle binds the executed script bytes to that hash across four cases. Target path is joined from registry without containment guard; registry is trusted input at this layer.

- [[Evidence/S2122]]
