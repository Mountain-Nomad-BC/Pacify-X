# Behavioral probe validation does not prevent effects or enforce a deadline

**Historical finding; current closure not revalidated.**

Duplicate IDs, missing control roles and invalid role labels accumulate reasons but every callback still runs. max_latency_ms is evaluated after invoke and compare complete, so it cannot stop a hanging callback. If all declared oracles have identical repr, discriminating is true even for constant output. Observed successful outputs are retained raw; only exception details are reduced to type.

- [[Evidence/S1631]]
