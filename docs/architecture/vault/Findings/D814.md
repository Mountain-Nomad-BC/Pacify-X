# Standard skill domain screening is a partial semantic filter

**Historical finding; current closure not revalidated.**

capability.json must be px-standard; restricted namespace values scanned only under selected key patterns. skill.yaml uses JSON parse or regex fallback rather than a full YAML parser. This screen is not full package contract validation; native admission remains necessary.

- [[Evidence/S1287]]
