# Export command success is process exit rather than structured verdict

**Historical finding; current closure not revalidated.**

_run_command inherits ambient environment while adding no-bytecode/no-user-site/PYTHONPATH, captures complete stdout/stderr, and checks returncode. It does not parse a JSON valid field or use ProcessSupervisor tree/capture accounting. Each timeout is separate.

- [[Evidence/S3626]]
