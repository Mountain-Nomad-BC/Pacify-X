# Security graph input seeds can exceed node budget

**Historical finding; current closure not revalidated.**

Initial seed set is built before traversal and is not trimmed to max_nodes. Depth zero can return too many seeds without truncation. Entire graph and all seed input are loaded before output budgets. Positive parameters lack fixed upper limits.

- [[Evidence/S1967]]
