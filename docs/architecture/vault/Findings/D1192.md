# Typed proposal aggregate assertion is check-only

**Historical finding; current closure not revalidated.**

Expected proposal candidate count compared only inside --check branch. Apply defaults to effects and appends even without that total assertion. Known surfaces/control IDs checked, but duplicate proposal assignments can create conflicting predecessor revisions because all planned from same snapshot.

- [[Evidence/S3356]]
- [[Evidence/S3352]]
