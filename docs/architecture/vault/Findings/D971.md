# Capped security graph may return dangling edges

**Historical finding; current closure not revalidated.**

Outgoing edge is appended before target-node cap check. At cap, target may be omitted while edge remains, so returned edge endpoints need not all appear in returned nodes. One-seed tests assert counts only.

- [[Evidence/S1968]]
- [[Evidence/S4168]]
