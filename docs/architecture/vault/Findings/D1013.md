# Disposition validator accepts some weakly shaped resealed records

**Historical finding; current closure not revalidated.**

Path check detects nonobject/duplicates but never tests nonempty string, so one empty path can pass. Owner/hash need truthiness only. Byte totals, digest syntax, inventory digest recomputation, disposition counts and expected-tree identity are unchecked; recomputed report seal can cover malformed values.

- [[Evidence/S2268]]
