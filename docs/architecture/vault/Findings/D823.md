# Reused materialized input has no shared-consumer lease

**Historical finding; current closure not revalidated.**

Deterministic target may be reused by independent operations, each with its own lifecycle receipt. Successful create can reclaim that shared path without a refcount/lease check for other consumers. Reattestation checks content, not concurrent usage.

- [[Evidence/S1316]]
- [[Evidence/S1318]]
