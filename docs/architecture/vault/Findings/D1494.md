# Completion writer tests mutate authoritative fixture paths

**Historical finding; current closure not revalidated.**

Both writer tests call owners against ROOT, then restore target bytes in finally. They assert final JSON and absence of prepared files; they do not inject mid-write crashes or concurrent changes. Restoration itself is a write and can overwrite intervening state. These tests were read only.

- [[Evidence/S4156]]
