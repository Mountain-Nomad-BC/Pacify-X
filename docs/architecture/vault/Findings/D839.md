# Skill editor validation is weaker than native package validation

**Historical finding; current closure not revalidated.**

Browser path checks cover basic absolute/traversal cases but not host byte/file/topology/case/device-name bounds. Contract links need an array without requiring each target exists; resource index checks listed files but not completeness. YAML fallback regex is not a full parser. Host materializer and admission must enforce their own contracts.

- [[Evidence/S336]]
- [[Evidence/S1322]]
