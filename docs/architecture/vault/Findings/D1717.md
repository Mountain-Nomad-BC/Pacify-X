# Owned acquisition fallback can remove a custom root without the normal gate

**Historical finding; current closure not revalidated.**

acquireWalkOwnership obtains lease before temporary allocation, a useful duplicate-owner boundary. If marking a custom makeTemporaryRoot result fails, catch directly rmSync recursively on that returned path rather than using safeOwnedEphemeralCleanup. Default allocation is mkdtemp; custom injection can widen this fallback target. Lease release follows best-effort removal.

- [[Evidence/S590]]
