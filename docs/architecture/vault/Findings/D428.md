# Semantic lint checks declarations and selected prefixes only

**Historical finding; current closure not revalidated.**

Required list containers are type-checked but elements are not; unhashable rows or nonstring observed effects can raise. Undeclared effects are rejected only for selected sensitive prefixes, using exact write membership rather than hierarchy. Concrete rollback requires only a truthy method; rollback.required false produces warning. Evidence arrays and observed effects are caller declarations, not admission or runtime interception.

- [[Evidence/S2425]]
