# Host lifecycle tests omit the reproduced outcomes

**Historical finding; current closure not revalidated.**

Tests cover duplicate active lease, one stale retry, fake worker success/error/timeout and residual process matches. They do not cover two stale failures or timeout followed by exit zero. A listenerCount>=0 assertion is tautological, and two matched PID fixtures do not establish a process-count bound.

- [[Evidence/S1445]]
