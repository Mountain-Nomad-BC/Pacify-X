# Parallel launch uses sequential wait budgets

**Historical finding; current closure not revalidated.**

Windows and Ubuntu are started before either wait, but each timeout begins at its own wait call. The second process may run during the entire first wait and then receive its full timeout. Launch/wait errors are retained after waiting existing children. Ordinary smoke result failures still allow the exhaustive member to run.

- [[Evidence/S3958]]
