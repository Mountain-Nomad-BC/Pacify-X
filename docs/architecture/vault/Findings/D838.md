# Skill identity synchronization can overwrite manifest-specific data

**Historical finding; current closure not revalidated.**

Synchronization merges missing defaults, updates capability identity/domain/schema and writes its full JSON to both capability.json and skill.yaml. Distinct YAML-only fields disappear. SKILL.md name update only handles LF opening frontmatter, while preparation catches errors and returns prior normalized draft.

- [[Evidence/S332]]
