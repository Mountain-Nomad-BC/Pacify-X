# Verified installed source is not rebound to final current bytes

**Historical finding; current closure not revalidated.**

The local identity is sampled once and cached through refreshes. Only unknown state receives late refresh; verified identity is not rechecked after effectful profiles or against final source bytes. Host matches=true can coexist with mismatch reasons, and protocol strings are not compared to exact supported values.

- [[Evidence/S666]]
- [[Evidence/S687]]
