# Installed syntax records can retain stale syntax-valid labels

**Historical finding; current closure not revalidated.**

AST parse errors append global errors but record syntax_valid is copied from source ownership or fallback True. syntax_valid_count subtracts only SyntaxError/UnicodeError strings, not every read failure. Selected map rows/counts are not full semantic certification.

- [[Evidence/S2778]]
