# Effect tests share discovery denominator with generated expected registry

**Historical finding; current closure not revalidated.**

Fixture expected records are produced by discovery itself. Tests cover selected literal unsafe calls/pruning; no alias, expression, nested scope or enforcement receipt cases. Locator hash test hashes a constant without invoking discovery.

- [[Evidence/S4192]]
