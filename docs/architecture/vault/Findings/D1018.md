# Exact duplicate exemption can misname nonempty package files

**Historical finding; current closure not revalidated.**

All matching paths ending __init__.py are classified empty-package-markers without testing empty content in classifier. Group byte equality proves they match each other, not that they are empty. Other classes likewise rely on fixed path sets and names.

- [[Evidence/S3070]]
