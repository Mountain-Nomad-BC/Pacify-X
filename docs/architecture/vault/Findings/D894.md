# Encoded working drafts get shallow parsed validation

**Historical finding; current closure not revalidated.**

Each encoded kind capped at 512KiB and total one MiB, then JSON parsed and envelope/schema/kind/draft-object checked. Inner draft structure is not recursively passed through inspectValue. View-state allowed keys also lack full type/enum checks.

- [[Evidence/S1341]]
