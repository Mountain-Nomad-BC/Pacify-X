# Memory repair steps are not ordered by the computed dependency order

**Historical finding; current closure not revalidated.**

Planner computes a topological node list but sorts repairs by defect class then node ID. Every node remaining after Kahn traversal is labelled dependency_cycle, including acyclic descendants blocked behind a cycle. Citation values need only a nonempty collection and temporal values need no date semantics. apply is planning data; no mutation occurs.

- [[Evidence/S2381]]
