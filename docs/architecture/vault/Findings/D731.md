# Repository twin merges provenance and lists simulations without running them

**Historical finding; current closure not revalidated.**

Static and runtime edges collapse into one set; edge endpoints become nodes so a manifest target only mentioned by edge avoids missing alert. Manifest relations are not added as edges. Simulation interface is a tuple of function names, source_of_truth false.

- [[Evidence/S2067]]
