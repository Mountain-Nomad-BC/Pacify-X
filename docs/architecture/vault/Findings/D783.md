# Graph connection labels may describe selected-neighbor edges

**Historical finding; current closure not revalidated.**

Per-node connection expression filters selected-node incident edges then falls back to item.degree only if result is zero. A neighbor may show its links to selected node instead of its full graph degree. Incident relations over 180 are grouped for label presentation.

- [[Evidence/S314]]
