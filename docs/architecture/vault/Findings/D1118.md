# Agency output can fail reporting after an outside-root write

**Historical finding; current closure not revalidated.**

Wrapper accepts absolute --output and writes it, then output.relative_to(root) for JSON summary raises if outside root. There is no pre-write containment check. Check-only mode can also fail at summary. No such operation was executed here.

- [[Evidence/S3439]]
