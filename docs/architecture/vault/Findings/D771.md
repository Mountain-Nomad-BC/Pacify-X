# Plugin rows require matching supplied inventory generation

**Historical finding; current closure not revalidated.**

Renderer accepts fresh or memory-current label only with equal compact/shard snapshot hashes; otherwise suppresses rows. No independent clock check occurs. It displays at most 120 rows but metrics describe supplied inventory; ACTIVE/DETECTED does not mean enabled.

- [[Evidence/S310]]
