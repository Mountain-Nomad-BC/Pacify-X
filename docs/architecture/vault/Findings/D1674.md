# Candidate JSON changes need explicit application

**Historical finding; current closure not revalidated.**

Typing canonical JSON marks draft dirty and saves a buffer, but studioEditorPayload uses structured draft while kind matches. Only studioApplyJson parses, normalizes and applies the text; predecessor-bound identity/version change is rejected. Save can therefore use the last applied structured definition, not newly typed raw text.

- [[Evidence/S348]]
- [[Evidence/S386]]
- [[Evidence/S402]]
