# Record errors do not prevent subsequent archive access

**Historical finding; current closure not revalidated.**

After verify_artifact_records returns errors, bind still opens first wheel/sdist paths from supplied records. Invalid filename, missing file or malformed archive can cause further reads or exceptions rather than a clean early refusal. No effect executed in audit.

- [[Evidence/S2899]]
