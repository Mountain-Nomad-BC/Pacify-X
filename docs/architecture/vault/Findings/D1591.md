# Engine outage publication is in-memory after rename

**Historical finding; current closure not revalidated.**

begin renames the whole runtime before returning a receipt; no durable intent, startup reconciler or automatic finally exists in this helper. Only cli.py and dashboard_api.py hashes are retained, so exact restoration claim covers those bytes, not every runtime file. Caller must ensure restoration after failure.

- [[Evidence/S523]]
