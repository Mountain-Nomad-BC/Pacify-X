# Active hash map allows collapsed contracts and trusted paths

**Historical finding; current closure not revalidated.**

Paths from map are joined without relative containment or symlink checks. Multiple active entries targeting same contract overwrite output dictionary; active_count counts distinct outputs, not entries. Empty active list passes. Rehashing current bytes does not itself prove those bytes admitted or behavior verified.

- [[Evidence/S3825]]
