# Retrieval readiness can pass without resource measurements

**Historical finding; current closure not revalidated.**

Missing p95/memory and missing maxima all default to infinity; infinity <= infinity passes. A pure probe with one perfect case and empty thresholds/metrics/state confirms complete=true with no resource measurements. This is evaluator behavior, not measured performance. 

- [[Evidence/S142]]
