# Environment scrub does not itself isolate network or executable search

**Historical finding; current closure not revalidated.**

Allowlist retains PATH/system locations and sets pip offline/config flags. Empty mapping uses ambient environment because of source-or-os.environ fallback. Offline argv construction returns a list and does not execute or apply a network boundary.

- [[Evidence/S2907]]
- [[Evidence/S2910]]
