# Partition merge failure can replace a good result with a partial file

**Historical finding; current closure not revalidated.**

Output is opened directly before row validation. A duplicate ID or malformed later record raises after earlier rows have been written, and summary is not updated as a transaction. Input file existence alone is checked before output mutation.

- [[Evidence/S3728]]
