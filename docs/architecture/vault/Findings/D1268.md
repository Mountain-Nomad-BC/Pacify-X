# Duplicate chain stages collapse before exact-denominator validation

**Historical finding; current closure not revalidated.**

Stage rows become a dictionary keyed by stage, so later duplicates replace earlier values. Equality of resulting keys to CHAIN_STAGES does not require exactly one row per stage. Generated receipt fragment references can fill stage evidence without a raw evidence artifact.

- [[Evidence/S3925]]
