# CSS audit scope omits cascade layer identity

**Historical finding; current closure not revalidated.**

Only media/supports/container preludes contribute scope. Layer and other at-rules are omitted; selector parsing strips comments lexically and splitSelectors tracks bracket/parenthesis depth without quote state. Matching selector strings can therefore collapse semantically distinct layer/declaration contexts.

- [[Evidence/S472]]
