# Manifest scalar extraction is not YAML semantics

**Historical finding; current closure not revalidated.**

Anchored regex reads matching key line from anywhere in manifest and strips quotes; comments, block scalars, nested structure and duplicate keys are not parsed as YAML. Priority sorting is lexical, so numeric-looking priority strings may not follow numerical order.

- [[Evidence/S3435]]
- [[Evidence/S3437]]
