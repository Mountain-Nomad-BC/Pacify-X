# Lazy hydration guarantees retained-state atomicity after full reads

**Historical finding; current closure not revalidated.**

prepare recursively reads body/reference text before checking total active count and byte footprint. Failure leaves active map unchanged under RLock, as tests cover, but huge inputs can allocate beyond retained budget before rejection. This is an in-memory context commit guarantee, not a pre-read byte/time bound.

- [[Evidence/S2325]]
- [[Evidence/S4245]]
