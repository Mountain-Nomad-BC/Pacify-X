# Scanner aggregation complete means parse completion rather than all-pass security

**Historical finding; current closure not revalidated.**

Every finding carries its envelope status; complete is simply not errors, so fail/blocked/skipped/uncertain scanner results can coexist with complete=true. Missing or empty input directory can also complete. The focused test explicitly expects aggregation completeness while preserving a fail count. 

- [[Evidence/S202]]
- [[Evidence/S4120]]
