# Typed inventory fuses historical semantics and current declarations

**Historical finding; current closure not revalidated.**

One historical surface overlay replaces the base surface after base SHA validation. Current action contracts and two controller navigation arrays add declarations; fixed mappings add structural/native/variant controls. The output is a hybrid provenance baseline, not a fresh rendered or executed inventory.

- [[Evidence/S3580]]
