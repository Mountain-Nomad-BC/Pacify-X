# Ollama catalog capacities are fixed declarations

**Historical finding; current closure not revalidated.**

Catalog maps all returned models and reports 32768 input and 8192 output tokens for each. Body/model count and model field schemas lack bounds; malformed response returns empty list. Token cancellation is checked initially but not subscribed for catalogue request.

- [[Evidence/S1162]]
