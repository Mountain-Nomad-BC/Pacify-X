# Bundle CLI is a dispatcher rather than certification owner

**Historical finding; current closure not revalidated.**

Unique label=path arguments dispatched to runtime builder/verification, printed result determines exit status with missing valid default true. Build has no dry-run mode here. Audit only inspected this entrypoint.

- [[Evidence/S3813]]
