# Exact action resolver can represent many instances

**Historical finding; current closure not revalidated.**

No suffix means all same-action matches form exact class; row variant accepts any dataset key containing id/index/row/key. Multiple named matches can also be exact. This does not mean unique element identity or every instance exercised.

- [[Evidence/S489]]
