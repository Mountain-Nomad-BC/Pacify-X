# Bridge conflict helper counts fixture invocations rather than mutation retries

**Historical finding; current closure not revalidated.**

Two child commands emit an error envelope with exits2 and1. no_mutation_retry is inferred from attempts==2 across those fixture calls, not from exercising a mutating operation retry path. request_bound_reason checks text rather than exact original request custody. Keep its claim limited to exit classification.

- [[Evidence/S699]]
