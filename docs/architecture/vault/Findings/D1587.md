# Efficiency ownership label exceeds the local implementation

**Historical finding; current closure not revalidated.**

The function creates a temporary tree and deletes it in finally without registering a marker/lease here. Scan worker ownership belongs imported discovery code; governor disposal is not in a dedicated finally if its request loop fails. Standalone report publication occurs only after measurement and cleanup succeed.

- [[Evidence/S482]]
