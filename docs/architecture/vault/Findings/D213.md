# Task plan persistence can retain partial final target after write failure

**Historical finding; current closure not revalidated.**

persist_task_execution_plan opens final identity path with xb and writes directly, without prepared replacement/fsync/failure cleanup. A failed write may leave an incomplete file that subsequent exact-byte replay rejects as collision. This differs from atomic replacement and WAL protocols elsewhere.

- [[Evidence/S3191]]
