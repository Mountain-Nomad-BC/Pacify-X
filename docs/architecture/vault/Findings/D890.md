# Ollama tests prove selected synthetic transport branches

**Historical finding; current closure not revalidated.**

Fixtures run loopback HTTP responses with no real model and fake VS Code objects. They assert stable faults and explicit done, but not real model capacity, redirects, catalogue, line-boundary chunking or all resource closure paths. Audit reads tests without running them.

- [[Evidence/S1433]]
