# Matched summaries do not pair trial identities or enforce frozen treatment

**Historical finding; current closure not revalidated.**

Task sets and per-task row counts must match, but run/trial IDs, environment hashes and comparison profiles are absent. Duplicate rows satisfy counts. Infrastructure-error rows remain in pass-rate denominator. Optional metrics can have different sample denominators and lack finite checks. Strict passed is True avoids truthy strings; result explicitly claims no statistical significance.

- [[Evidence/S1645]]
