# Reconciliation tests omit live normalization and failing apply sequences

**Historical finding; current closure not revalidated.**

Tests cover important planner branches and mocked status, but do not run full ledger append normalization, history compaction, duplicate stage rows, error-based demotion suppression, nonprimary card states or multi-phase partial commits. Their source does not prove those paths safe.

- [[Evidence/S4331]]
