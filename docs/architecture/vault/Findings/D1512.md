# Hardware dashboard fixtures test cache integrity with a mocked probe

**Historical finding; current closure not revalidated.**

Tests check miss/hit/forced/expired states, malformed JSON rebuild, tampered report hash, future timestamps and an upper TTL rejection. hardware_report is mocked, so these are cache lifecycle tests rather than measurements or hardware compatibility evidence.

- [[Evidence/S4173]]
