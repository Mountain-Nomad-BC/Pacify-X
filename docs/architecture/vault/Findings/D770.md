# Catalog badges and session liveness need precise interpretation

**Historical finding; current closure not revalidated.**

Several broker/lifecycle/hash/parallel labels are constants. Active enterprise session count requires active status and age at most five minutes, without rejecting future heartbeat timestamps. Registered, runnable and running counts have separate meanings.

- [[Evidence/S309]]
