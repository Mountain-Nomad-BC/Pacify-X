# Behavior tree identity depends on aliases and sanitized path strings

**Historical finding; current closure not revalidated.**

Tree digest concatenates alias:path:hash lines in caller root order after path sanitization. Aliases/delimiters are not constrained and normalized path collisions are not rejected. The digest does not bind extractor version or extraction configuration. Output inside a scanned tree can enter later scans.

- [[Evidence/S3780]]
