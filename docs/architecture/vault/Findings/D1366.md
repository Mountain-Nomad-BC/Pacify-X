# Mechanism scan uses combined lexical matches rather than semantic independence

**Historical finding; current closure not revalidated.**

Combined regex emits nonoverlapping matches and alternation order can hide a second mechanism matching the same span. It scans line by line with replacement decoding, so cross-line patterns are not found. Declared prefilter/individual-regex/overlap tables are not used by the active _scan_text path. Metadata names/descriptions inspect only the first 12000 bytes. 

- [[Evidence/S022]]
- [[Evidence/S043]]
- [[Evidence/S038]]
