# Historical token parsing chooses first resolvable file

**Historical finding; current closure not revalidated.**

Command regex extracts one whitespace-delimited script token; semicolon alternatives return first existing file. Numeric anchors/fragments stripped. File paths can be outside root; resolve hides leaf symlink before later is_symlink check, so physical-file wording does not reject all aliases.

- [[Evidence/S3396]]
- [[Evidence/S3382]]
