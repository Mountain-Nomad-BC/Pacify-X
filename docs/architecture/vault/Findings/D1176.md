# Installed adapter identity reads are not one snapshot

**Historical finding; current closure not revalidated.**

Receipt, VSIX, identity and control files are read at separate times; final receipt hash rereads after interpretation. No final identity revalidation binds all artifacts atomically. receipt/vsix relative_to(root) checks happen at output assembly after earlier reads.

- [[Evidence/S3506]]
- [[Evidence/S3501]]
