# Streaming line cap also applies to combined chunk buffer

**Historical finding; current closure not revalidated.**

After decoding, the entire buffered chunk is byte-counted before splitting lines. A chunk containing many individually small valid lines can exceed line limit and fail, even below total stream limit. Decoder replacement behavior is not configured fatal.

- [[Evidence/S1154]]
