# Loopback port readiness is not process identity

**Historical finding; current closure not revalidated.**

reserveLoopbackPort listens on port zero then closes before VS Code starts, leaving a reuse race. probeCdp accepts HTTP200 and any truthy webSocketDebuggerUrl without matching expected host/PID/port. Its 1-second timeout is socket inactivity, not an overall response deadline; an ongoing body is sliced to 16KiB while waiting for end.

- [[Evidence/S612]]
