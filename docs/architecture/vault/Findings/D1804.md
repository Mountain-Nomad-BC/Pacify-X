# Project reconstruction equality is selected metadata rather than graph content

**Historical finding; current closure not revalidated.**

Project identity compares path, revision, inventory digest and counts, not graph bytes or validation/errors. Build result acceptance is broadly truthy. Initialization cancellation from one entry is shared across two attempted controls; a common build result contributes across multiple entrypoints.

- [[Evidence/S773]]
