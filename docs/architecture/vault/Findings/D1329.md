# Partition merge assumes sorted inputs and keeps a global ID set

**Historical finding; current closure not revalidated.**

heapq.merge is applied to streams without checking each stream is ordered. It avoids retaining all rows but stores every seen ID, so memory is still proportional to corpus identity count. Output input-aliasing is not rejected; lazy input iterators can open after output truncation.

- [[Evidence/S3727]]
