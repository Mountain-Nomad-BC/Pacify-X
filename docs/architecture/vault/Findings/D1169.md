# Assembled inventory hash names a different digest scope

**Historical finding; current closure not revalidated.**

control_chains.inventory.path names operational_surface_inventory.json, while its sha256 is source manifest source_sha256, not inventory file SHA256. This field must be interpreted with that specific schema convention, not assumed to authenticate named inventory bytes.

- [[Evidence/S3373]]
