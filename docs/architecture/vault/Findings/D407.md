# Reasoning strategy required modes are ranking hints and its plan is not execution

**Historical finding; current closure not revalidated.**

Required recognized modes gain100 then remain subject to max_modes truncation; unknown required modes are ignored. Recursive prerequisite expansion can exceed max_modes and introduce evidence, which is not a selectable mode/handler. Generic stop conditions are returned strings. No mode-to-operation translation, payload creation or execution occurs.

- [[Evidence/S1915]]
