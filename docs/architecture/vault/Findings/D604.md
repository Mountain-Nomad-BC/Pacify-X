# Authority sidecar stores input contract but deliberately omits runtime values

**Historical finding; current closure not revalidated.**

Workflow envelope validates run_inputs mapping but stores contract descriptors and runtime_input_values_stored=False, not actual run inputs. Default contracts derive undriven input ports; supplied nonempty list replaces inference. Execution inputs arrive at run/start/resume separately.

- [[Evidence/S3099]]
- [[Evidence/S3107]]
