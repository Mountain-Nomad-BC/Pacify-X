# Fault campaign aggregate test does not call the aggregate owner

**Historical finding; current closure not revalidated.**

One test loads campaign metadata and checks dimensions, command tokens and referenced test paths. The failed-lane test writes its own two-row JSON fixture and asserts Python all is false; it never invokes run_release_fault_campaign publication or its failure handling.

- [[Evidence/S4353]]
