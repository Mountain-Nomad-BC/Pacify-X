# Quickstart validity and selection rely on result shape

**Historical finding; current closure not revalidated.**

The demo treats absent valid as true and applies bool to present values, then assumes selection capability_ids has an element. Its framework_validation label contains processing-order status rather than full validation. This is explicitly a pre-validation demonstration.

- [[Evidence/S295]]
