# Specialty count deduplication differs from category rows

**Historical finding; current closure not revalidated.**

Every queue entry is appended, while represented IDs and top-level counts use sets. Duplicate IDs across rows/categories can remain visible while counts understate rows. Active/mapped state is catalog membership alone; loading_rule prose does not enforce admission or lazy reading.

- [[Evidence/S3573]]
