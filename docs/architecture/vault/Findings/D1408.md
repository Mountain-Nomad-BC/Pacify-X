# Model normalization selects first alias and infers defaults

**Historical finding; current closure not revalidated.**

First present alias wins even if null; contradictory aliases are not compared. Hidden/query/KV divisibility is checked and missing KV heads default to query heads with a warning. Explicit head_dim and architecture-specific projection dimensions are not consumed; returned formulas use hidden/query.

- [[Evidence/S174]]
