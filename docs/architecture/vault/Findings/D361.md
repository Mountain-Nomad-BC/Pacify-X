# Knowledge rollback replaces status metadata before its receipt exists

**Historical finding; current closure not revalidated.**

Rollback CAS-checks current candidate and verifies the retained signed target, writes a fresh head, then separately writes receipt. It does not carry suspect/revalidation metadata forward; an unchanged candidate hash can still permit status replacement. Actor emptiness and target hash grammar are not early explicit guards. No rollback receipt recovery transaction is present.

- [[Evidence/S2300]]
