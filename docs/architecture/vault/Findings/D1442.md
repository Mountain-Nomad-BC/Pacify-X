# Startup emits a tooling command that the parser rejects

**Historical finding; current closure not revalidated.**

Startup guidance emits python -m runtime.cli tooling --project path, but tooling requires the assess subcommand. Pure parser probe returns exit2 for that emitted shape. Existing startup test checks deferred metadata but does not parse or execute its suggested command.

- [[Evidence/S1846]]
- [[Evidence/S1746]]
- [[Evidence/S4146]]
