# Lifecycle stage attribution can survive failed sequencing

**Historical finding; current closure not revalidated.**

The builder credits all required stages from one valid same-operation observation or shared invalid-transition flag; missing action is treated as rejected transition. Run-control selectors omit exact run/kind, and the profile has no universal finally to stop/release unfinished work. Per-kind merge can replace prior identity observations.

- [[Evidence/S764]]
