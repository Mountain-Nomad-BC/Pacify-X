# Manifest verification needs a separately trusted required denominator

**Historical finding; current closure not revalidated.**

Empty evidence list with recomputed self-hash can pass; schema/version/role/generation semantics are not enforced. Optional-missing entries skip verification even if a file later appears. Only selected records covered, and manifest valid/errors fields are outside its digest payload. Certificate caller separately authenticates manifest file.

- [[Evidence/S2919]]
- [[Evidence/S2833]]
