# Policy consistency does not establish enforcement reachability

**Historical finding; current closure not revalidated.**

Policy index body paths must equal top-level JSON files excluding index files, and startup IDs must be known. This does not check complete policy semantics or runtime enforcement callers; duplicate IDs/bodies collapse into sets.

- [[Evidence/S3078]]
