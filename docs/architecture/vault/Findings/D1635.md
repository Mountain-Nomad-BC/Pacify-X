# Forced snapshot refresh joins or follows the current publication

**Historical finding; current closure not revalidated.**

A forced caller waits for an in-flight nonforced promise then requests a forced pass; ordinary callers share one promise. Each awaiting caller posts to its own target afterward. Rejection of the first promise prevents that forced retry path. There is no workspace generation binding around assigning shared currentSnapshot.

- [[Evidence/S1106]]
- [[Evidence/S1117]]
