# Authoritative quarantine detects post-move identity changes but requires explicit recovery

**Historical finding; current closure not revalidated.**

Intent is exclusively written/fsynced before os.replace. Destination snapshot must match size/mtime/device/inode/hash; mismatch or final receipt failure retains custody and tries recovery receipt, falling back to intent if recovery write fails. There is no rollback or recovery executor here. Destination-existence check and replacement are separate; no shared lock closes producer/path races. Tests exercise identity swap and final receipt failure.

- [[Evidence/S1618]]
- [[Evidence/S4098]]
