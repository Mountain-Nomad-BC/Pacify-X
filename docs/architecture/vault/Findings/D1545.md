# Extension recycle tests use a deletion adapter rather than OS trash

**Historical finding; current closure not revalidated.**

Success fixtures call fs.rm even when asserting useTrash=true; they prove adapter option plumbing and receipt logic, not recoverability through the operating-system recycle bin. Tests cover link/hardlink rejection, changed trees, partial/uncertain outcomes, original-path replacement and Unicode depth. They omit quarantine ancestor redirection and receipt publication failure after effects.

- [[Evidence/S1374]]
