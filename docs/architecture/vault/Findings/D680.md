# Corrective malformed records can escape structured error output

**Historical finding; current closure not revalidated.**

Top-level nonobject reaches .get; nonobject card is recorded as error but final blocking_open still calls .get on every card. Unhashable dependency/status/priority shapes can also raise. Strict finalizer exception may return valid while blocking_open remains nonzero.

- [[Evidence/S1959]]
