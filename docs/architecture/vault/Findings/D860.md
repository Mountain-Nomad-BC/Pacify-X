# Activity tail limits do not bound file reading or parsing

**Historical finding; current closure not revalidated.**

Entire JSONL is read and split, every valid line parsed until syntax failure, and only then sliced to tail. JSON shape/schema is not validated before integrity/filters; valid JSON such as null can make read paths throw.

- [[Evidence/S868]]
