# Lifecycle reconciliation writes before final denominator checks

**Historical finding; current closure not revalidated.**

Specialty state map collapses duplicate IDs and accepts arbitrary state strings. Script writes specialty/packages/ledgers before discovering some missing records, and writes catalog before checking every specialty matched. It does not update through signed SkillStudio lifecycle transaction.

- [[Evidence/S3910]]
