# Current evidence index apply publishes before validity return

**Historical finding; current closure not revalidated.**

CLI calls publish_index then returns 0/2 according to value.valid. Core publisher writes both locations regardless of validity using separate prepared replacements. One target can update before failure of second; CLI invalid result does not undo publication.

- [[Evidence/S3452]]
- [[Evidence/S2090]]
