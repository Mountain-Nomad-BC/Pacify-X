# Empty MCP context can pass the probe and support runtime_verified

**Historical finding; current closure not revalidated.**

Production definition and probe pass an empty context path. contextEnvelope falls back to an empty object; the context snapshot uses a truthiness fallback, so it returns that object. Probe accepts truthy structuredContent. Middleware records success for every resolved handler value, and observed runtime status checks event metadata rather than useful payload content. The evidence proves narrower invocation conditions.

- [[Evidence/S1060]]
- [[Evidence/S840]]
- [[Evidence/S530]]
- [[Evidence/S1149]]
- [[Evidence/S1151]]
