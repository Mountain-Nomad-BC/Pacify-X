# Enterprise state, event and doctor receipt have separate commit points

**Historical finding; current closure not revalidated.**

initialize can persist missing or migrated state. mutate writes sealed state before appending its event; doctor then writes a separate receipt. load substitutes defaults after read/parse errors, which does not itself establish preserved recovery of malformed existing data. No one transaction spans these writes.

- [[Evidence/S987]]
