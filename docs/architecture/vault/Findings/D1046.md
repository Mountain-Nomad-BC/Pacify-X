# Doctor receipt identity is digest-named rather than authenticated immutability

**Historical finding; current closure not revalidated.**

Receipt target uses report hash and transaction ID uses first24 characters. Existing path immutability/idempotence relies on WAL behavior; helper has no existing-receipt equivalence check. Returned byte digest is a separate read after commit, not signed provenance.

- [[Evidence/S2757]]
