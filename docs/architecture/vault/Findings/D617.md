# First migration can fail before its first recoverable journal

**Historical finding; current closure not revalidated.**

Snapshots and workspace/user backup copies occur before main journal creation. Failure leaves snapshot custody that causes a later no-journal run to refuse legacy state. Partial prepared-skills directory skips rebuild and can later be published before missing package detection.

- [[Evidence/S3744]]
