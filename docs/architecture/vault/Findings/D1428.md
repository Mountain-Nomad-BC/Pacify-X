# External requirement cards have positional rather than stable identity

**Historical finding; current closure not revalidated.**

Card IDs use sorted record sequence, so adding an earlier path renumbers subsequent cards. Duplicate paths/rows and declared hash grammar are not checked. validation=complete follows integrated/reference labels, and bool(cards) only prevents vacuous empty completion.

- [[Evidence/S3766]]
