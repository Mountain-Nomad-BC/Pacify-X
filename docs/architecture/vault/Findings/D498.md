# Split contract IDs are used as paths before containment proof

**Historical finding; current closure not revalidated.**

Contract IDs are interpolated into JSON targets with no identifier validation; dump writes before relative_to(root) is evaluated. Source-derived traversal risk requires malformed input; not reproduced. Existing .agents/.px namespace split persists.

- [[Evidence/S264]]
- [[Evidence/S266]]
