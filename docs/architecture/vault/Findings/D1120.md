# Profile apply reports success while extra profiles remain

**Historical finding; current closure not revalidated.**

Profile reconciler detects projected names absent from owners, but does not remove them. Apply result is always true after writes, even with extras; check mode rejects them. Missing source/projection directories can yield empty successful result because glob returns no names.

- [[Evidence/S3547]]
