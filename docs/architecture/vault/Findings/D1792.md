# Lifecycle result predicates omit expected run and revision identity

**Historical finding; current closure not revalidated.**

Many predicates accept operation schema/status and any run ID. Pause/cancel/stop durable-run receipts need no expected terminal state; rollback accepts a rolled-back object. Pure probe accepted another run with failed state for pause. Later status waiting adds checks but earlier operation.valid remains true after subsequent failure.

- [[Evidence/S762]]
- [[Evidence/S764]]
