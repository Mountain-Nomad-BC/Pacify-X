# Export validates then rereads mutable files

**Historical finding; current closure not revalidated.**

Run validation is followed by fresh records and manifest reads, so checked bytes are not retained throughout export. Unknown requested conversation IDs yield blank rows; duplicate conversation sources use last mapping. Output uses existence check then ordinary write without atomic reservation.

- [[Evidence/S3252]]
- [[Evidence/S3254]]
