# Logic duplicate signature omits decorators and short functions

**Historical finding; current closure not revalidated.**

Only functions with at least three body statements are hashed. Root function name/decorators and all type_comment/type_params fields are removed, while local identifiers remain. Decorated or generic functions with different semantics can normalize together; short or renamed-local equivalence can be missed.

- [[Evidence/S3072]]
