# Support matrix validation uses declarations and text presence

**Historical finding; current closure not revalidated.**

Compares project requires-python/classifiers to policy, then checks CI text for expected minor/runner strings. It does not execute CI jobs or validate parsed job matrix topology. Certificate platform fields are checked as supplied strings, not cryptographically measured platform identity.

- [[Evidence/S2914]]
