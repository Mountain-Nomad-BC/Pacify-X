# Graph writer leaves stale extra artifacts in place

**Historical finding; current closure not revalidated.**

Core writer creates output and writes expected files, without removing preexisting extras or post-write validation. Separate validate_graph_artifacts requires exact JSON file set and expected bytes. Wrapper success can therefore precede a later stale-extra failure.

- [[Evidence/S3568]]
- [[Evidence/S2220]]
