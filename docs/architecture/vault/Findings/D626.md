# Skill packaging tests partly reuse implementation corpus

**Historical finding; current closure not revalidated.**

Expected file coverage calls generator._owned_files, sharing its exclusions. The test loop named facades iterates .px/skills, not .agents/skills. Manifest projection counts are checked, but no artifact build/install occurs in this test file.

- [[Evidence/S4384]]
