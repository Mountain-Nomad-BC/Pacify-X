# Commissioned skill reconciliation can miss non-row drift

**Historical finding; current closure not revalidated.**

Changed detection uses first matching current row and stale ID set; duplicate identical rows or top-level metadata drift can leave check valid. Apply directly rewrites complete expected file only when row delta exists and returns true without reread.

- [[Evidence/S3861]]
