# Segment migration is a separate historical projection with partial publication

**Historical finding; current closure not revalidated.**

Migration reads events under read_events lock then releases it before fingerprinting and writing segments/manifest. Concurrent append can change legacy identity; final equivalence validation can fail after new files exist. Existing differing manifest refuses overwrite. Legacy JSONL is retained and remains active authority.

- [[Evidence/S2550]]
- [[Evidence/S4298]]
