# Candidate resource completion checks counters but not valid flag

**Historical finding; current closure not revalidated.**

After complete_process, four counters must equal zero; resource_status.valid is not required. Lifecycle proof field is set true after selected checks. Non-timeout exceptions/cancellation lack unconditional terminate/reconcile; late command can spawn before remaining deadline becomes zero.

- [[Evidence/S3981]]
