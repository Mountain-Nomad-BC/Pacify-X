# Offline improvement evidence remains supplied input

**Historical finding; current closure not revalidated.**

The offline comparison checks distinct actor/optimizer/judge strings and matched held-out score records, requiring an improvement within the regression allowance. It returns quarantined_candidate and requires_independent_promotion=True. It neither runs the experiment nor verifies the independence of those identities.

- [[Evidence/S1932]]
