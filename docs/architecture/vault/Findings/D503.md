# Read-only declared helpers have incomplete input and resource bounds

**Historical finding; current closure not revalidated.**

Inventory enumerates before count limit and reads whole files at arbitrary resolved target; scan allows empty patterns and unbounded matches. Rank defaults to zero weights, malformed metrics can raise uncaught AttributeError and NaN scores are not rejected.

- [[Evidence/S2022]]
- [[Evidence/S2021]]
- [[Evidence/S2020]]
