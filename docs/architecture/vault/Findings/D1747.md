# Graph gesture evidence is narrower than named gesture families

**Historical finding; current closure not revalidated.**

plusMinus tests plus only and zeroOrHome tests zero only. Arrow focus checks inequality, which can pass if focus leaves nodes. Synthetic wheel/pointer events do not independently prove native input routing. Other graph cases use before/after transform datasets, a useful but bounded observation.

- [[Evidence/S545]]
