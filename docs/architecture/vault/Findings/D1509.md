# Dashboard runnable counts are stored-state observations

**Historical finding; current closure not revalidated.**

Tests create unsigned minimal admission JSON with decision admitted/runnable_state runnable and minimal session/run state JSON, then expect runnable revision and run counts. Display totals therefore must not be interpreted as authenticated admission, current executable readiness or completed operations.

- [[Evidence/S4176]]
