# Restart flags and stage requirements are separate tables

**Historical finding; current closure not revalidated.**

Editor configuration sets requires_restart true, but persistence/reload_reopen stages are not required by the later kind set. Consumers that only use stage_policy can have different obligations from consumers using restart flag. This is a declaration inconsistency to account for, not evidence a restart happened.

- [[Evidence/S3541]]
