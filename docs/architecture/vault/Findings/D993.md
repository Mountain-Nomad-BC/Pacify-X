# Exact filesystem comparison misses deletions and outside effects

**Historical finding; current closure not revalidated.**

Snapshot hashes files under temp except __pycache__. Changed list iterates only after entries, so deleted files are absent and unreported. Writes outside temp, metadata-only changes and effects in excluded caches are not observed. Symlink targets may be followed by is_file/read_bytes.

- [[Evidence/S2107]]
- [[Evidence/S2113]]
