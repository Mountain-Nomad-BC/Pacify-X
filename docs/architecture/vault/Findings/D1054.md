# Frozen artifact manifest validation is shallow

**Historical finding; current closure not revalidated.**

Record validation requires keys and package target but not path safety, field types, required/generated booleans, digest grammar, byte-size values or unique records. Empty record list and arbitrary skill_projection mapping can pass with valid flags and correct self-digest.

- [[Evidence/S2876]]
