# Shared pytest base guard observes only a directory during call

**Historical finding; current closure not revalidated.**

The runtest_call wrapper obtains the shared base, then recreates it and forces an exception if it is no longer a directory. It does not verify contents, identity, or deletion during all setup/teardown phases.

- [[Evidence/S4062]]
