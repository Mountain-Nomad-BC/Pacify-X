# CLI parser and dispatch denominators are independently accounted

**Historical finding; current closure not revalidated.**

Pure parser construction yields69 top-level commands and169 leaf parsers. AST inspection finds a corresponding top-level dispatch branch for every command. Leaf count retains positional action choices rather than counting them as executed routes. Hardcoded section/profile choices are an additional CLI exposure boundary over registry data.

- [[Evidence/S1842]]
- [[Evidence/S1712]]
