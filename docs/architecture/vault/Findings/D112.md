# Portable context flags describe construction rather than universal policy enforcement

**Historical finding; current closure not revalidated.**

buildContextEnvelope includes no file bodies and copies locators, objective and coordination metadata. It labels credentials_included false and supplies a numeric context_injection_cap_tokens, but performs no general secret scan or token calculation over those caller fields. codexPrompt states Git and claim rules as host instructions; the module does not intercept future commands. Its environment filter removes a fixed list of key names.

- [[Evidence/S938]]
- [[Evidence/S937]]
