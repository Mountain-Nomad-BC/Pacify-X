# Historical live-process and reparse checks have time and resolution gaps

**Historical finding; current closure not revalidated.**

Each script snapshots process command lines once and matches path substrings, missing users of files whose paths are absent from command lines and later processes. GetFullPath is lexical; relative link targets are resolved against current directory, not their containing link directory. Only the generated-fixture script explicitly rejects a top-level reparse target. No immediate content snapshot is compared before removal.

- [[Evidence/S3651]]
- [[Evidence/S3661]]
- [[Evidence/S3655]]
