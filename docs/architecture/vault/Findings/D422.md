# Introspection validates selected shape rather than causal evidence

**Historical finding; current closure not revalidated.**

Event IDs must be unique but whitespace is accepted; timestamps and summaries require presence, not parsed time/typed contents. Timeline sorts timestamp text, which need not match instant order across offsets. Decision references need only be nonempty, not resolve to events. Invalid traces still produce summaries and compare_alternatives drops valid/errors. No trace capture, authenticated event order or outcome validation occurs.

- [[Evidence/S2417]]
