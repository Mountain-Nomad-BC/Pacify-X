# Capture tests verify proposal state without proving durable accepted origin

**Historical finding; current closure not revalidated.**

The semantic candidate fixture uses a dry-run capture and supplied semantic envelope, then checks candidate/proposal/retrieval-disabled state and rejection of a quarantined label. Capture apply testing checks schema and repeat event ID, not a crash/restart chain or byte-for-byte immutability of every repeated file.

- [[Evidence/S4265]]
