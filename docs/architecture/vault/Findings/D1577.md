# Template completeness schema checker implements a subset

**Historical finding; current closure not revalidated.**

The local helper checks type, const/enum, pattern/minimum, required/additionalProperties and items. It does not implement references, combinators, max/unique/minItems or format; successful fixtures are not full JSON Schema validation. Historical 318-file counters and completeness/index flags are retained metadata checks.

- [[Evidence/S4413]]
