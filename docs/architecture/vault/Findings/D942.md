# Exact lock parsing is narrower than package semantics

**Historical finding; current closure not revalidated.**

Requires exact pin and lowercase SHA-256 syntax, handles continuations and rejects interrupted continuation. Name normalization lowercases and replaces underscore only; versions are nonwhitespace strings, not fully parsed package-version semantics. Empty lock returns invalid with possibly empty errors.

- [[Evidence/S2916]]
