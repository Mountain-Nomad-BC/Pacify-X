# Frozen DAG inventory does not make every list exact

**Historical finding; current closure not revalidated.**

The validator enforces 43 distinct node records and 86 distinct dependency edges, matching known cards and a topological dependency order. Topological_order is checked by set equality without list length/uniqueness, so a duplicate terminal entry passes; confirmed with a pure synthetic 43-node/86-edge graph. Consumer lists must include reverse dependencies but may contain extra known consumers without reverse dependency.

- [[Evidence/S3857]]
