# Stable samples need valid counts and stable identity

**Historical finding; current closure not revalidated.**

Settlement counts consecutive boolean acknowledgements, not the same element or source revision. Required sample count0 or negative can accept unacknowledged state; the report pure probe reproduces zero-sample acceptance. Scope/capability references can also become detached after preparatory clicks.

- [[Evidence/S750]]
