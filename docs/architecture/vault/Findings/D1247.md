# Duplicate modern artifact paths update only their first occurrence

**Historical finding; current closure not revalidated.**

Rows are captured before mutation. On mismatch, next(...) finds first artifact with matching path, so later duplicate entries can retain stale digest while apply reports valid when no errors. Coverage separately rejects duplicate resolved paths. Existing tests do not include duplicate entries.

- [[Evidence/S3903]]
- [[Evidence/S2455]]
- [[Evidence/S4322]]
