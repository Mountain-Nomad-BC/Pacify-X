# Provider negative probes can certify weak or empty behavior

**Historical finding; current closure not revalidated.**

Prompt-log checks pass with empty logs; correction check only proves old value absent from search and can pass when search always returns empty, without proving corrected value retrievable. Global-slot probe compares configured namespace strings and Python object identity, not real process/storage separation. foreign_write_denied actually writes through foreign instance and checks local value unchanged. Tests use an in-memory fake and typed failure injection.

- [[Evidence/S2714]]
- [[Evidence/S4315]]
