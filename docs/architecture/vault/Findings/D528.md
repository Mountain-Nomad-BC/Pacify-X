# Syntax normalization is narrower than behavioral equivalence

**Historical finding; current closure not revalidated.**

JSON/YAML key sorting removes insertion order and duplicate keys collapse. Python AST drops comments/locations though source locations can be observed. Unsupported types retain bytes; supported parse/serialization errors propagate.

- [[Evidence/S3017]]
