# Licensing report checks relative location after writing

**Historical finding; current closure not revalidated.**

Caller can supply absolute destination; writer creates parents and writes report before relative_to(root) is evaluated for returned locator. Outside-root destination therefore can receive bytes before ValueError. CLI default uses repository evidence path.

- [[Evidence/S2332]]
- [[Evidence/S1854]]
