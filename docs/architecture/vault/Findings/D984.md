# Browser readiness can use file version without launch

**Historical finding; current closure not revalidated.**

Windows browser file-version metadata yields ready with no version requirement or process launch. Other platforms use --version. Neither path checks that the browser can render the certification app or that required automation features work.

- [[Evidence/S1683]]
