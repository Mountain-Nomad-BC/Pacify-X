# Studio coercion is broader than field names imply

**Historical finding; current closure not revalidated.**

_strings accepts arrays but stringifies nonstrings. Workflow required/approval flags use bool and retry counts int, so string false and fractional inputs change meaning before controller checks. Layout validation is stricter: exact nodes, x/y fields, no bool, finite coordinates and absolute bound.

- [[Evidence/S3101]]
- [[Evidence/S3102]]
- [[Evidence/S3103]]
