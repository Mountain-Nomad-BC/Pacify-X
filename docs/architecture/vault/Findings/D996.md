# Repeat normalization can remove meaningful differences

**Historical finding; current closure not revalidated.**

JSON keys containing time, created, updated, duration or timestamp are removed recursively, in addition to selected volatile names. This can hide timeout/runtime or other semantically meaningful fields. Fixture-root redaction is case-insensitive substring replacement, not path-token equivalence.

- [[Evidence/S2108]]
