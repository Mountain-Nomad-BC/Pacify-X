# External evidence verified means manifest and inventory hashes

**Historical finding; current closure not revalidated.**

The validator checks contained outer manifest and inventory digest/bundle ID, without parsing listed payload contents. Inventory path comes from manifest and is joined without the same traversal/containment check. Strict mode requires available manifests, but an empty records list is not rejected by this function.

- [[Evidence/S2172]]
