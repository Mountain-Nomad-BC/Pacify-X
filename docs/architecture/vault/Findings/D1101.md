# Git revision and custom command interfaces assume trusted callers

**Historical finding; current closure not revalidated.**

Revision is appended as a raw argv item without separator or revision resolution; option-like values may be interpreted by Git. Custom command_builder can return an arbitrary fixture command. Timeout check only requires positive value; caller still owns authorization and acceptable bounds.

- [[Evidence/S3407]]
- [[Evidence/S3413]]
