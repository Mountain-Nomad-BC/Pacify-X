# Legacy agent topology is separate from the active graph renderer

**Historical finding; current closure not revalidated.**

upgradeLegacyAgentTopology and drawLegacyAgentTopologyEdges remain defined, but the modern upgradeAgentTopology route uses typed builder graph. Legacy edge selector expects data-agent-section while legacy created nodes use data-legacy-agent-section. The mismatch is a dormant helper boundary, not evidence that modern graph edges fail.

- [[Evidence/S453]]
