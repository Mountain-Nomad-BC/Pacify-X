# Layout rule cap is per call rather than global retained ownership

**Historical finding; current closure not revalidated.**

Constructable stylesheet applies fixed numeric clamped attributes, reducing CSS injection through those attributes. However its 2048 limit counts this apply invocation; retained rules for still-connected elements can exceed that across partial calls. querySelectorAll materializes candidates before the cap is applied.

- [[Evidence/S441]]
