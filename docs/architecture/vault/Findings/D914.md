# Installed Studio smoke partly bypasses editor interaction

**Historical finding; current closure not revalidated.**

Requires installed source modules directly, creates bridge/approval material and calls setup/creation functions with confirmCreate true. It checks catalog reopening and run outcomes, but receipt knowledge_ui_bridge_cli_backend true is not evidence of an actual webview editor click path.

- [[Evidence/S1470]]
