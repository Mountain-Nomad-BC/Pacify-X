# Object and array descriptor checks are asymmetric

**Historical finding; current closure not revalidated.**

Plain object properties require enumerable data descriptors and reject dangerous keys. Array density/key count/symbol checks do not inspect element descriptors before every/iteration accesses them. Real webview structured transport limits expressible values; this helper alone is not a hostile-object sandbox.

- [[Evidence/S1351]]
