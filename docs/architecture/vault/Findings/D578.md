# Fault campaign loses partial evidence on timeout and binds inputs afterward

**Historical finding; current closure not revalidated.**

All lanes run after nonzero exits, but timeout exceptions abort receipt publication. Captured output is discarded after hashing. Only tests/ argument tokens are hashed, after execution; transitive code, interpreter, policy and in-flight input identity are not bound.

- [[Evidence/S3996]]
