# Cache marker creation and cache reads enforce different custody checks

**Historical finding; current closure not revalidated.**

Read paths lstat root/marker and reject links, while ensure creates/writes through the selected root without equivalent root/ancestor identity checks. Existing marker owner/schema are checked, but direct read-modify-write has no lock or atomic publication and can lose concurrently retained versions.

- [[Evidence/S526]]
