# A global search debounce crosses independent surfaces

**Historical finding; current closure not revalidated.**

Activity, memory query/project/source and catalog search share one searchTimer. Typing on another surface before 250ms cancels the earlier scheduled query; callbacks retain the old input object. This is a single browser scheduling lane, not independent surface work.

- [[Evidence/S403]]
