# Wheel installation uses before and after hashes around separate pip read

**Historical finding; current closure not revalidated.**

Wheel path is resolved and hashed, pip later reads it, then helper hashes again. Byte replacement and restoration during that window is not detected by endpoint hashes. No immutable copy/handle binds exact installed bytes.

- [[Evidence/S2896]]
