# Import mirror policy can point outside the project

**Historical finding; current closure not revalidated.**

Canonical and mirror strings are joined to root without resolve/containment checks; absolute paths or parent segments can redirect reads. Mirror verification is byte equality only. Source traversal omissions remain outside findings, while parse failures become findings and fail CLI. 

- [[Evidence/S132]]
