# Lease stale ownership is PID-based and release is best-effort

**Historical finding; current closure not revalidated.**

Signal-zero EPERM means alive; other probe failures mean dead. There is no process birth identity, compare-and-unlink predecessor check or fsync. release sets its released flag before reading/unlinking and suppresses errors, preventing retry after a transient failure.

- [[Evidence/S524]]
