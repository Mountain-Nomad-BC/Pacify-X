# Section chunk reuse depends on stored receipt integrity and declared input hash

**Historical finding; current closure not revalidated.**

Caller reuses only passed receipts matching section, chunk ID and input_sha256. The reader separately checks exact keys/schema, member count, bounded output-evidence shape and an unkeyed content digest. Reuse is tied to declared inputs and retained evidence; it is not a fresh execution or an authenticated external attestation.

- [[Evidence/S1763]]
- [[Evidence/S3197]]
