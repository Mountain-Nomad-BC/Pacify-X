# Workflow zero positions and edge positions disagree

**Historical finding; current closure not revalidated.**

Node layout preserves zero using nullish fallback, while drawWorkflowCanvasEdges uses position.x || 40 and position.y || 40. An inert DOM probe produces source endpoint M 218 76 for a node at 0,0 instead of 178,36. The source discrepancy affects visual connection alignment.

- [[Evidence/S458]]
- [[Evidence/S340]]
