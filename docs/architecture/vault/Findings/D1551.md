# Timeout followed by zero exit can report completed

**Historical finding; current closure not revalidated.**

The worker records termination_reason=timeout but classifies numeric zero as completed. The explicit timeout error is limited to a null code. Unchanged source in a fake child/terminator VM produced status completed, timeout reason and both closure booleans true.

- [[Evidence/S524]]
