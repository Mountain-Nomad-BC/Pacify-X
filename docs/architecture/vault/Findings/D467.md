# Classified JSON containment is enforced only on the corrupt-authority path

**Historical finding; current closure not revalidated.**

load_classified_json loads/validates caller path before any allowed-root check; successful files and derived-corruption results can be outside allowed root. Optional validator TypeError/ValueError is treated as corrupt state, even if caused by validator logic. No production caller found in runtime/scripts search; registry owner existence does not wire this loader into every state store.

- [[Evidence/S1619]]
- [[Evidence/S1620]]
