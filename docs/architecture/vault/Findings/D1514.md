# Memory dashboard states are classification fixtures rather than attachment operations

**Historical finding; current closure not revalidated.**

Tests distinguish detached, configured empty, expired and new-project onboarding; mocked workspace-monitor and project-map values suppress unrelated attention for new repositories while retaining invalid existing-map warnings. They do not initialize, attach or renew actual memory sessions.

- [[Evidence/S4177]]
