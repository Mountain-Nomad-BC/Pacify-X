# Proof-matrix preflight compares counts rather than exact contracts

**Historical finding; current closure not revalidated.**

Before effects, main compares only proofMatrix.controls.length and inventory.control_count. Equal-count substituted IDs, policy/evidence modes, duplicate controls or manifest drift are not rejected there. The matrix file is reread for a final digest after effects, which can describe different bytes than the parsed schedule.

- [[Evidence/S668]]
- [[Evidence/S692]]
