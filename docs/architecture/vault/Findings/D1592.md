# Outage restore verification follows the effect

**Historical finding; current closure not revalidated.**

Restore requires original absent, renames displaced runtime back, then checks two hashes. If a hash check fails, restored remains false but the original path now exists, so the next restore call fails its boundary guard. Parent/root identity is not revalidated immediately before either rename.

- [[Evidence/S523]]
