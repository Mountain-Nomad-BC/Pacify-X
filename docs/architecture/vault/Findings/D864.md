# Activity redaction depends on field names

**Historical finding; current closure not revalidated.**

Secret/content-like keys are redacted with depth/array/key caps; arbitrary sensitive text under an innocuous key is not detected. Relative scopes are lexical rather than realpath identities. Captured-content false is a schema declaration, not arbitrary-secret proof.

- [[Evidence/S863]]
