# Corrective acceptance syntax checks do not prove runnable target behavior

**Historical finding; current closure not revalidated.**

Pytest form may have no target and checks only tokens beginning tests; selector existence/other paths/options not validated. Python script form checks file existence without resolved root containment. CLI allowlist is fixed including historical version strings.

- [[Evidence/S1958]]
