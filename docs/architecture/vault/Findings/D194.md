# Workflow fail-closed handling waits for the selected batch

**Historical finding; current closure not revalidated.**

All futures are awaited and successful outcomes checkpointed before failed_closed, lifecycle signals or worker errors are handled. A sibling may continue through its attempts after another has failed. This preserves settled sibling progress, but fail-closed is a batch boundary rather than immediate cancellation.

- [[Evidence/S3297]]
