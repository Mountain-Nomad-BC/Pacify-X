# Interaction profiles and mutation classification use labels

**Historical finding; current closure not revalidated.**

Supplied surface/control profiles can override inference. Otherwise regexes classify labels such as remove/run/config, even when an action only edits a draft. This is conservative metadata, not actual effect observation.

- [[Evidence/S488]]
