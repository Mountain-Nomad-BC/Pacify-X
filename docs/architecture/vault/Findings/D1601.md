# Fault walk counts only controls already rendered in a prior UI receipt

**Historical finding; current closure not revalidated.**

The selected set intersects supported UI kinds, rendered IDs in a zero-error UI receipt and required failure/recovery policy. It binds current source manifest and emits input receipt/matrix hashes, but unrendered controls never enter this denominator. A zero eligible set can still produce a successful process.

- [[Evidence/S646]]
