# Graph clustering bounds retained nodes after consuming the full iterable

**Historical finding; current closure not revalidated.**

build_graph_clusters materializes every node before slicing max_initial_nodes and iterates every edge. Duplicate IDs overwrite in lookup while enumerated_nodes counts the original selected rows. BFS treats edges as undirected, splits components at cluster size and retains first32 lexical terms rather than semantic novelty. Missing endpoints include nodes excluded by truncation.

- [[Evidence/S2379]]
