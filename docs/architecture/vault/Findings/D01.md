# Workflow declarations and runtime execution differ

**Historical finding; current closure not revalidated.**

workflow_execution_bindings explicitly distinguishes executable_runtime and executable_validator. graph_registry nevertheless emits executes edges from listed project-stream skills. execute_project_stream invokes the registered workflow handler, not each listed skill body. Treat those generated executes edges as declared composition unless the handler proves invocation.

- [[Evidence/S1501]]
- [[Evidence/S2221]]
- [[Evidence/S2688]]
