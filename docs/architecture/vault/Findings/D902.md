# Control source manifest authenticates bytes rather than handler semantics

**Historical finding; current closure not revalidated.**

Checks exact lexical inventory path, schema, per-surface ordered-ID hash and unique IDs; resolves source files and hashes them. Source reference symbols/handler backend are copied or described, not AST-validated. Filesystem containment uses resolve/stat without realpath alias rejection.

- [[Evidence/S484]]
