# Palette disappearance can reuse a stale command identity

**Historical finding; current closure not revalidated.**

Command selection normalizes visible labels rather than contributed command IDs/publisher. When the palette disappears the decision helper may retain last valid identity and continue Enter dispatch; acceptedPrompt can accept closure without observing that prompt. Focus or tab changes can redirect keyboard effects.

- [[Evidence/S712]]
