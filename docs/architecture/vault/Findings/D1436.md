# Mining publication can stop between independently written control surfaces

**Historical finding; current closure not revalidated.**

Order is catalog; per-skill packages; admission ledger; mining admission; workflow registry/projection; mining receipt; source coverage; engineering lifecycle; every package hash refresh. Direct writes have no joint preflight, lock, WAL or rollback, and overwrite retained wave entries. Idempotent wording does not imply transactional recovery or preservation of newer metadata.

- [[Evidence/S3749]]
