# Smoke wait helper cannot bound a hanging predicate

**Historical finding; current closure not revalidated.**

waitFor checks elapsed time between awaited predicates. A predicate consuming shell execution stream can itself hang past the nominal timeout. Debug wait result is not checked before exercised-live label; task timeout rejects but listener cleanup only occurs on expected event.

- [[Evidence/S1472]]
- [[Evidence/S1475]]
