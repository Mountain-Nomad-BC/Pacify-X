# Punch-card refresh can partially write before later error

**Historical finding; current closure not revalidated.**

Errors list is shared across sorted records. Dirty records are written only when no errors have yet accumulated; a later malformed/missing record prevents subsequent writes but does not roll back earlier ones. Final valid=False can coexist with earlier accepted-record mutations. Writes are direct and have no retained revision/atomic group.

- [[Evidence/S3900]]
- [[Evidence/S3905]]
