# Candidate queue merges by ID with uneven requirements

**Historical finding; current closure not revalidated.**

CSV duplicate skill IDs overwrite earlier rows. Manifest-only IDs start with candidate/defer but omit CSV required_before_promotion list. Multiple unsorted manifest glob entries may overwrite same fields. No duplicate/schema/digest validation establishes one authoritative source.

- [[Evidence/S3436]]
