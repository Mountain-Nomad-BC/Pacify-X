# Hybrid retrieval test signals are supplied rather than computed

**Historical finding; current closure not revalidated.**

Each optional signal changes an unrelated baseline into a hit by replacing one input field. This proves fusion consumes six signal types, not that an embedding model, graph scorer or external service computed them. The adapter subprocess fixture has no timeout; import portability is also checked by source strings.

- [[Evidence/S4221]]
