# Aggregate selector membership varies with current gap dispositions

**Historical finding; current closure not revalidated.**

Selectors enumerate current disposition rows, filter typed kind/label/regex, and union gap_ids. Operational promotion can remove those gap IDs and change desired child set despite fixed plan bytes. This makes historical plan plus current state the real input, not plan SHA alone.

- [[Evidence/S4048]]
- [[Evidence/S4041]]
