# Observer failed shutdown blocks the same API retry path

**Historical finding; current closure not revalidated.**

disable requires state active and stores blocked on native failure; subsequent disable rejects blocked. enable rejects any retained state, including disabled/uninstalled. Successful stop precedes commit, whose failure leaves persistence behind native state.

- [[Evidence/S2579]]
- [[Evidence/S2578]]
