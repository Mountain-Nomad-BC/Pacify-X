# ZIP hazard inventory inspects central-directory claims without payload validation

**Historical finding; current closure not revalidated.**

Archive hash is computed, but member bodies are never opened; CRC values, sizes and ratios are metadata. Nested archives are only listed and do not themselves trigger danger. Traversal/encryption/link/ratio findings produce recommendations, not extraction, move or deletion. CLI returns zero after writing even when archives contain recorded errors.

- [[Evidence/S3690]]
- [[Evidence/S3695]]
