# Exact gap-event fallback hashes first matching raw event

**Historical finding; current closure not revalidated.**

If direct file missing, helper scans ledger lines, skips malformed JSON and hashes stripped text of first matching event ID. It does not independently validate event chain in that function or require matched event to support same card. Earlier snapshot validation is a separate read.

- [[Evidence/S3397]]
- [[Evidence/S3389]]
