# Deep host tool input bypasses target checking

**Historical finding; current closure not revalidated.**

visit returns any value at depth greater than eight without examining it. An unchanged selected-function VM with fake filesystem rejects a top-level C:/outside/secret path but retains the same target nested nine times and records no validated target pointers. No tool or filesystem effect was invoked.

- [[Evidence/S1119]]
