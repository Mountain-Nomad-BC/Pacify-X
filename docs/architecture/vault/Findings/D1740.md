# Absent numeric bounds are converted to zero

**Historical finding; current closure not revalidated.**

Number(await getAttribute(min/max)) converts null to zero. For ordinary number inputs without attributes, alternate selection may fail to exercise the field despite a valid editable control. The harness needs explicit missing-bound semantics.

- [[Evidence/S554]]
