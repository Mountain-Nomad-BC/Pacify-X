# Cleanup preexistence rejection can still recursively remove existing fixture parents

**Historical finding; current closure not revalidated.**

runInstalledCleanupProfile rejects preexisting fixed fixture paths but its finally recursively removes both fixture parents whenever present. It has no created-this-run guard, so rejection itself can lead to deletion of preexisting contents. This was source-reviewed only; no cleanup was executed.

- [[Evidence/S783]]
