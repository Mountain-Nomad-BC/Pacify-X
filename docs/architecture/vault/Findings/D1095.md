# Git worktree attributes are distinct from working-tree contents

**Historical finding; current closure not revalidated.**

Archive command uses selected revision, default HEAD. --worktree-attributes controls attribute interpretation but does not include dirty/untracked source bytes. Report keeps revision string without resolving commit or retaining archive SHA256.

- [[Evidence/S3408]]
