# Portable routing and transcript wrappers delegate authority to selected runtime

**Historical finding; current closure not revalidated.**

They prepend local source only when a runtime directory exists, otherwise use imported installed modules and framework_root. Specialist routing returns recommendations and valid-based exit, not spawned agents. Transcript export forwards explicit apply and exits zero if the underlying call returns.

- [[Evidence/S220]]
- [[Evidence/S237]]
