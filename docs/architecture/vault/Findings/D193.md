# Workflow retries reuse inputs and one consumed approval

**Historical finding; current closure not revalidated.**

Approval is consumed before _execute_node, and the same signed host-consumed metadata is passed to every retry. retry_limit repeats failed attempts without new evidence or idempotency reservation. Output name/type validation follows the retry loop, so those errors do not receive the same retry policy.

- [[Evidence/S3297]]
- [[Evidence/S3291]]
- [[Evidence/S4428]]
