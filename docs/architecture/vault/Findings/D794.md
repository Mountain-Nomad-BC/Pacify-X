# Browser ordering deliberately permits revision regression with newer clock

**Historical finding; current closure not revalidated.**

Only lower revision combined with nonnewer generated time is rejected. Lower revision/newer timestamp and same revision/older timestamp can render; latest values then replace prior ordering state. Browser test explicitly exercises first behavior.

- [[Evidence/S467]]
- [[Evidence/S1451]]
