# Inventory lifecycle IDs depend on historical list position

**Historical finding; current closure not revalidated.**

Lifecycle identities use path.1, path.2 and so on. Reordering or inserting historical lifecycle labels changes identity-to-meaning association. Stable names for actions coexist with positional lifecycle IDs; consumers must evaluate metadata revisions rather than assuming identical ID preserves semantics.

- [[Evidence/S3584]]
