# Fixture tests prove repeatability without full independent ZIP validation

**Historical finding; current closure not revalidated.**

The test runs builder twice at shared generated paths, compares file hashes and checks leading ZIP signature and size range. It does not independently extract every member/CRC, install in VS Code or impose a subprocess timeout. Shared paths can race under concurrent owners.

- [[Evidence/S1448]]
