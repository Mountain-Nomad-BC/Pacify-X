# Cache discovery fails closed on walker limits but excludes by a distinct policy

**Historical finding; current closure not revalidated.**

The Python cache owner uses100000 files, depth128 and2GiB observed file size, with links skipped. Repository-root pytest cache and canonical external custody are pruned after removing cache components from the classification path. Directory matching itself is case-sensitive while suffix matching is casefolded. This differs from the extension scanner.

- [[Evidence/S3665]]
- [[Evidence/S1648]]
