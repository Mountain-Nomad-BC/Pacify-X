# Activity append admits parseability rather than prior hash integrity

**Historical finding; current closure not revalidated.**

recordActivity refuses malformed JSONL but does not call eventIntegrity before append. A parsed tampered or state-divergent log can receive a new event linked to state.last_event_sha256. Appending event and replacing state are distinct commits.

- [[Evidence/S865]]
