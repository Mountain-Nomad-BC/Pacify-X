# Placement publication chooses identity field by insertion order

**Historical finding; current closure not revalidated.**

publish_placement_artifact scans reversed artifact items for any valid sha256-suffixed field and uses the first found as filename; it need not be the whole artifact digest. Existing file comparison detects mismatch, but absent-file check then replacement does not atomically reserve an immutable name across writers. Work-plane admission supplies scheduling, not file identity authentication.

- [[Evidence/S2147]]
