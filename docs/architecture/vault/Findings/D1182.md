# Assembler tests retain gaps but do not authenticate origins

**Historical finding; current closure not revalidated.**

Tests show incomplete control remains observed_only, one exact synthetic direct record completes it, and observed true can complete without attempted. They reject contextual kind, outside IDs and stale source, but not replayed timestamps, policy-only changes, conflicting runs or fabricated nonempty references.

- [[Evidence/S4096]]
