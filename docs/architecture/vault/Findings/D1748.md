# Operational boolean and process success use different predicates

**Historical finding; current closure not revalidated.**

Per-record operational depends solely on completeChain, ignoring probe.errors. Receipt operationally_complete compares operational count to all records; process exitCode is set only for aggregate errors. Missing controls/chains can therefore leave exit zero, while errors may coexist with operational true.

- [[Evidence/S571]]
