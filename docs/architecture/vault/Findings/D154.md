# Approval is consumed before downstream operation outcome

**Historical finding; current closure not revalidated.**

Both RSA host approval and workflow approval create an exclusive marker before returning to the executor. A subsequent marker write/signing or downstream operation failure does not unconsume the proof. Retries need the caller to distinguish consumed authorization from successful operation evidence.

- [[Evidence/S3114]]
- [[Evidence/S3116]]
