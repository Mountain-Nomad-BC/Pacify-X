# Tar membership checks ignore nonregular entries

**Historical finding; current closure not revalidated.**

Inspector skips links/directories/special entries and checks only limited case-sensitive forbidden prefixes plus four required regular names. It does not reject duplicate names, traversal/normalized aliases or malicious link targets. No extraction occurs in auditor, so these are proof limits rather than an extraction exploit claim.

- [[Evidence/S3414]]
