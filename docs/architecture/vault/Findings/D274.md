# Stream timeout is observed after synchronous handler returns

**Historical finding; current closure not revalidated.**

timeout_seconds is range-checked but no timer preempts handler. Only after handler returns does elapsed time or expired lease produce failed result; prior side effects remain. The test sleeps beyond budget and asserts failure after return. Handler lookup uses workflow ID and optional replacement mapping; binding handler string is a report label rather than callable identity verification.

- [[Evidence/S2688]]
- [[Evidence/S4313]]
