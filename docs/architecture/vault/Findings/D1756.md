# Suspended-frame test accepts render acknowledgement without completed frame work

**Historical finding; current closure not revalidated.**

Test replaces requestAnimationFrame with a no-op and waits for a graphRendered message with positive counts/dimensions. It proves the fallback acknowledgement remains live, not that all chunked graph rendering or paint completed. This matches the graph evidence boundary recorded in batch96.

- [[Evidence/S1398]]
