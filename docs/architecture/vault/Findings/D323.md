# Python startup audit does not acquire a coherent store snapshot

**Historical finding; current closure not revalidated.**

State, memory and events are stat-checked then read separately without a shared coordination lock or final identity pin. Missing state returns valid configured=false. Byte caps bound observed sizes and event count5000, while session file enumeration and recursive DAG depth have different resource limits. Concurrent publication can yield a mixed observation or grow files between stat and read.

- [[Evidence/S3064]]
- [[Evidence/S3061]]
- [[Evidence/S3062]]
