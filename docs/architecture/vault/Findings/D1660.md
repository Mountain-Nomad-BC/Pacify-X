# Generic lifecycle success accepts an empty object

**Historical finding; current closure not revalidated.**

Operation-specific checks explicitly require passed/admitted/eligible/run identity for many operations. The fallback merely rejects failure words in status/state/decision, so an empty object for recover is accepted. VM probe confirms this; it does not establish recovery or durable outcome correctness.

- [[Evidence/S351]]
