# Coverage compaction deliberately reduces attribution evidence

**Historical finding; current closure not revalidated.**

Keeps top-level contexts only for configured module suffixes and removes nested function/class contexts everywhere. Uses fixed .compacted replacement without lock/fsync. Result preserves totals but cannot support discarded per-test attribution.

- [[Evidence/S2851]]
