# Intake publication can outlive failed project-state update

**Historical finding; current closure not revalidated.**

Intake replace precedes state parse/update/history/replace. Shared fixed .next names, no lock or WAL, and separate history hash/read allow races. Scan occurs before apply/state existence gate; quarantine decision does not physically quarantine tooling.

- [[Evidence/S3228]]
