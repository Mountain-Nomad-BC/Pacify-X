# Installed smoke checks selected live surfaces and cardinalities

**Historical finding; current closure not revalidated.**

Driver checks sidebar ready/render acknowledgment, nonzero graph geometry and selected live versus Python snapshot counts. These are concrete host observations when executed, but count equality is not semantic equivalence and geometry does not prove all graph interactions.

- [[Evidence/S1476]]
