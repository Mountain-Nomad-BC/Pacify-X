# Deep links can use positional subtask fallback

**Historical finding; current closure not revalidated.**

The resolver first tries exact task/subtask identity, then extracts digits from a dotted suffix and indexes a parent subtask list. A stale or noncanonical identifier can therefore display a positional sibling. The record is a read-only snapshot projection, not task identity authorization.

- [[Evidence/S407]]
