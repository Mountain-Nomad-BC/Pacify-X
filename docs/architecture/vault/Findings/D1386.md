# Empty missing backlog fails after output creation begins

**Historical finding; current closure not revalidated.**

CSV fieldnames are taken from rows[0] after the CSV file is opened for writing. Zero missing rows raises IndexError and can leave a truncated file. Markdown is written later, and CLI compares row count with declared missing count only after both outputs; there is no paired atomic publication.

- [[Evidence/S055]]
