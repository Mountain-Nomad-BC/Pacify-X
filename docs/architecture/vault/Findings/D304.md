# Startup count and worker bounds are not whole-operation resource bounds

**Historical finding; current closure not revalidated.**

Capability, policy and catalog limits are checked after materialization; model/project data have no analogous count cap. Explicit tool_names bypass default candidate limit8. ThreadPoolExecutor limits parallel workers but waits for all callbacks without deadline, including at context exit. Configured coordination startup also reads retained events/memory, despite no skill-body hydration.

- [[Evidence/S3056]]
- [[Evidence/S3064]]
- [[Evidence/S3237]]
