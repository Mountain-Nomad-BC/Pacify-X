# Communication repetition can hide differing messages

**Historical finding; current closure not revalidated.**

Optional same category/casefold repeat_key keeps first text and unions evidence/source IDs; no text equivalence check. Caller can remove mandatory categories. Input/bytes unbounded; digest excludes dropped corpus. Insufficient budget preserves all mandatory records and reports invalid.

- [[Evidence/S2781]]
