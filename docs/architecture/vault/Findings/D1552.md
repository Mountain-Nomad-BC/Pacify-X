# Termination rejection can escape timeout settlement

**Historical finding; current closure not revalidated.**

Signal handlers discard the termination promise; timeout attaches then without a rejection handler. A rejecting terminator can produce unhandled rejection and a hanging one can leave the outer worker unresolved. Windows taskkill helper itself has no timeout. No live termination was attempted.

- [[Evidence/S524]]
