# Metadata truncation can obscure requested identities

**Historical finding; current closure not revalidated.**

Missing IDs consume selected positions. First oversized record stops processing, so later requested IDs may never be examined or reported missing; valid can be true with no returned record. max_records has no fixed ceiling.

- [[Evidence/S2153]]
