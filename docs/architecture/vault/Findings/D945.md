# Toolchain identity can combine two different interpreter identities

**Historical finding; current closure not revalidated.**

Optional executable path is hashed, but Python version/platform fields come from current running process. Filesystem case-sensitivity field is based on path normalization behavior rather than probing actual target filesystem.

- [[Evidence/S2911]]
