# Supply-chain writer test uses declared artifact hashes

**Historical finding; current closure not revalidated.**

The test passes invented wheel/sdist digests and no artifact files, then checks output types, one checksum, version and a provenance subject. Its final hash assertion merely checks a nonempty computed string. It does not verify real artifact bytes, signatures or complete source provenance.

- [[Evidence/S4360]]
