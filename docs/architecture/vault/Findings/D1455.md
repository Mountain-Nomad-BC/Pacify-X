# Full profile claim precedes section validation and uses required group status

**Historical finding; current closure not revalidated.**

Full/release claims full_profile before checking current section receipts, so later gate failure follows the failed-stage path. group_status.valid only checks configured required groups; if those are current, optional noncurrent groups do not trigger refresh. The executed profile command becomes the cross-group coexistence command, rather than the original full member command.

- [[Evidence/S1769]]
- [[Evidence/S3195]]
