# Coverage validates freshness and bytes without rederiving route semantics

**Historical finding; current closure not revalidated.**

For active tier A/B routes, consumer checks kind, healthy label, time window, contained receipt path and SHA256 equality. It does not rerun health producer or bind local auxiliary policy files in that branch. A hash-correct receipt supports byte identity but not every derived healthy claim.

- [[Evidence/S2457]]
