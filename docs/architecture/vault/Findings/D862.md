# Local state reads can retain evidence while failing

**Historical finding; current closure not revalidated.**

readState preserves corrupt bytes and receipt before throwing, so the read path can write local evidence. Semantic checks on parsed state are incomplete; unlocked readers can observe log/state between separate publications.

- [[Evidence/S862]]
- [[Evidence/S871]]
