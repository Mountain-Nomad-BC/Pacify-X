# Cache CLI output is an additional unguarded publication path

**Historical finding; current closure not revalidated.**

Even preview can write caller-selected --output, creating parents and replacing a file. This path has no collision/alias restriction against scanned input. Successful return always exits0 and the result has no valid field; errors raise before that return.

- [[Evidence/S3664]]
