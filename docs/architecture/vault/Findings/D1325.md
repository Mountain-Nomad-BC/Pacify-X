# Near-duplicate prefix bucketing loses close neighbors

**Historical finding; current closure not revalidated.**

SimHash candidates are only compared within equal extension and equal first four hex digits. A one-bit difference in that prefix prevents comparison even when full Hamming distance is within threshold. Normalized-hash equality is a separate exact grouping path.

- [[Evidence/S3673]]
