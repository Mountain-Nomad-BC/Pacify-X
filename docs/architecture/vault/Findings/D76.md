# An authoritative-named trace helper uses substring correlation

**Historical finding; current closure not revalidated.**

authoritative_runtime_path tests the requested correlation string anywhere in json.dumps(event), skips malformed JSON or TypeError and sorts string timestamp values. Matching rows are useful navigation evidence but do not establish an exact causal trace or full malformed-record accounting.

- [[Evidence/S008]]
