# Classifier binds product bytes separately from evidence/control

**Historical finding; current closure not revalidated.**

Evidence/control records carry no content hash; suffix/name allowlisting is not payload validation. product_valid excludes nonproduct errors, while valid rejects them. Harness digest is curated subset, not complete transitive implementation closure.

- [[Evidence/S2807]]
