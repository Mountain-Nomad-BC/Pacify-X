# Archive metadata and content digest are read at different times

**Historical finding; current closure not revalidated.**

Both archive tools hash then independently parse/stat archive bytes, without a stable before/after check. A changing archive can yield a content digest paired with later metadata. The simple catalog reads whole archive bytes into memory; hazard inventory streams its digest but loads metadata.

- [[Evidence/S3736]]
- [[Evidence/S3691]]
