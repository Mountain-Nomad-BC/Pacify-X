# Staging overwrites reserved output names without a source reservation check

**Historical finding; current closure not revalidated.**

Materialization writes audit README, SHA256SUMS, replay contract and manifest into copied tree. Optional VSIX artifact names are checked for case-insensitive uniqueness and extension, but archive format and aggregate payload size are not validated there. Copies and hashes occur at separate times.

- [[Evidence/S3632]]
