# Workflow trace remains an identity projection

**Historical finding; current closure not revalidated.**

The browser trace uses workflow/version/run identity, with a hash optional unless both compared records provide one. Node receipts are placed by node ID, so the final duplicate replaces earlier entries. Missing receipt list can clear node state. This projection does not validate event chains/signatures.

- [[Evidence/S443]]
