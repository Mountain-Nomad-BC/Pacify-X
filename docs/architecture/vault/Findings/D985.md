# Readiness dependency parity is version-label parity

**Historical finding; current closure not revalidated.**

Lock v3 root name/version/direct dependency mappings must match and direct pins must look exact. Installed direct package.json version is compared, but bytes/integrity hashes, transitive installed closure and executable scripts are not verified. Registry package names are joined as paths without additional containment grammar.

- [[Evidence/S1687]]
