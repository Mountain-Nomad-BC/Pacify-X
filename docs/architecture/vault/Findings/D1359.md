# Incomplete audit omits excluded and oversized sources from completion

**Historical finding; current closure not revalidated.**

Custody/generated directories and nonselected/large/link files are skipped without full physical denominator. os.walk traversal failures are unreported; stat and initial raw read occur outside the catch. Python AST reparses the file in a second read, while lexical rules use prior bytes, allowing version mismatch during mutation. 

- [[Evidence/S011]]
