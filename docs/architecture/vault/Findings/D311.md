# Scheduler pressure and ownership are caller-fed transient facts

**Historical finding; current closure not revalidated.**

Missing pressure becomes0; NaN bypasses threshold comparisons and caller agents count replaces internal heartbeat count. Path normalization is lexical and omits canonical alias/traversal resolution. Maps lack synchronization or persistence, while project dispatch instantiates a new scheduler each time. This is local admission bookkeeping, not durable cross-owner capacity custody.

- [[Evidence/S3001]]
- [[Evidence/S2631]]
