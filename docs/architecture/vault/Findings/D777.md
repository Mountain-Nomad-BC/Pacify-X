# Activity classification trusts producer liveness and vocabulary

**Historical finding; current closure not revalidated.**

Renderer deduplicates stale correlations and excludes seven terminal status strings from active rows, but timed_out is absent. It does not recompute agent heartbeat age. Missing capture policy can produce an ACTIVE presentation; supplied integrity flags determine badges.

- [[Evidence/S312]]
