# Identifier-only audit intentionally never claims comprehensive validity

**Historical finding; current closure not revalidated.**

Five controls are always not_run, so valid remains false even when scoped_valid passes. Four pattern/archive gate statuses depend on hit lists rather than errors; read failures can coexist with those individual passed gates while scoped_valid is false. CLI exits1 for noncomprehensive valid, not merely for identifier findings.

- [[Evidence/S3403]]
