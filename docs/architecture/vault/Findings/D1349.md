# Import audit matches source strings rather than resolved dependencies

**Historical finding; current closure not revalidated.**

Python uses AST import names; JS uses a literal regex missing side-effect imports, dynamic imports and runtime resolution. Selection casefolds suffix but parser branch tests exact .py, so .PY takes JS scanning. Any target beginning three dots is marked escape even when package-relative syntax is legitimate. 

- [[Evidence/S131]]
