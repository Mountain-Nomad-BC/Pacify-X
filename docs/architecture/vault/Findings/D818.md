# Production warning text overstates the unverified branch

**Historical finding; current closure not revalidated.**

The same reportPostCommitWarning callback is invoked for invalid commit receipt. Extension callback logs and warns that the immutable revision committed, even though host coordinator classified outcome unverified. Catalog recovery is needed to resolve that uncertainty.

- [[Evidence/S1305]]
- [[Evidence/S1014]]
