# Canonical record writing trusts manifest before checking provenance

**Historical finding; current closure not revalidated.**

Writer loads run manifest without verifying its digest/source-copy bytes first, then binds records against its supplied sources/queue. Records and manifest write separately using overwrite-capable write_text after earlier exists check; no lock/reservation protects concurrent writers.

- [[Evidence/S3245]]
