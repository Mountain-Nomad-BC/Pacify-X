# Exact model identity checks are explicit but initial calls precede the deadline

**Historical finding; current closure not revalidated.**

The route must contain exact model ID, pacify-local must bind exact vendor, and selected ID/vendor/family/version are checked. selectChatModels and initial countTokens occur before starting the bounded request timer. A stalled initial host API call is therefore outside that local deadline.

- [[Evidence/S1015]]
