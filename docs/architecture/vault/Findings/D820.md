# Materialized input publication is direct final-directory construction

**Historical finding; current closure not revalidated.**

Target combines sanitized identity/version plus hash suffixes. Existing trees require exact content and topology; new trees written directly with exclusive files and verified afterward. Concurrent writer can encounter incomplete target and fail rather than wait; failure retains partial tree.

- [[Evidence/S1316]]
