# Lifecycle history is synthesized from one reconciliation event

**Historical finding; current closure not revalidated.**

All intermediate lifecycle entries between current and target receive the same timestamp, reason and evidence references. This is an evidence-backed projection of claimed progress, not a record showing separate admitted implementation, test and integration executions. Existing at-target/closed cards return without refreshing evidence.

- [[Evidence/S3850]]
