# Host tool policy constrains declared inputs rather than actual implementation effects

**Historical finding; current closure not revalidated.**

The owner unions all grant effects and scope roots, rejects effects outside read/inspect/query/observe and requires non-billable, deny/loopback-only and no credential namespace. Recognized path/URI strings are checked and rewritten to physical paths, but the tool implementation receives no OS/network sandbox from this function. Undeclared internal effects remain outside these input checks.

- [[Evidence/S1119]]
