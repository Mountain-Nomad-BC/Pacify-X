# Control selectors can alias distinct indicators

**Historical finding; current closure not revalidated.**

Multiple indicators resolve to the same metric card, provider container, cleanup summary or graph-state element. Direct resolution proves the first selector match is visible, without validating the requested state/value. Semantic fallback ranks substrings; the token id matches invalid with score one.

- [[Evidence/S546]]
- [[Evidence/S547]]
