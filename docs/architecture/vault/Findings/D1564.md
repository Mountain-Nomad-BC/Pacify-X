# Manual installer verifies a selected hash manifest rather than signed release authority

**Historical finding; current closure not revalidated.**

The installer derives expected package/version, chooses first filename-regex match in SHA256SUMS and hashes the VSIX once. It does not authenticate the manifest, enforce exact whole-line filename membership or recheck bytes immediately after install. Hash integrity and certified publication authority are distinct.

- [[Evidence/S299]]
