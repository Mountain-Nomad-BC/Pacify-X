# Provider policy request binding and numeric checks have gaps

**Historical finding; current closure not revalidated.**

Requires provider_id but does not compare it with request. Negative expected charge passes safe-integer/max check; both missing budget IDs can match. Unknown fallback rank labels compare as -1 and may pass equal unknown primary labels.

- [[Evidence/S1171]]
