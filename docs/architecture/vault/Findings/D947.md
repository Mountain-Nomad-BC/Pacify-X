# Release environment validity and lock validity can diverge

**Historical finding; current closure not revalidated.**

Validator copies lock errors but not false lock.valid as error, checks installed distribution versions and declared support/current minor. Empty lock can therefore yield valid true with lock_valid false when other checks pass. It does not verify installed package bytes or require current OS in the environment path.

- [[Evidence/S2915]]
