# Static service audit completeness omits skipped files and traversal errors

**Historical finding; current closure not revalidated.**

Unreadable/oversized/unsupported/link files are skipped without an error denominator; os.walk has no onerror. stat occurs outside the read catch and may abort. complete is hardcoded true, and CLI exits zero with review findings. The test covers only one IPv4 config and an added resolver line. 

- [[Evidence/S129]]
- [[Evidence/S4125]]
