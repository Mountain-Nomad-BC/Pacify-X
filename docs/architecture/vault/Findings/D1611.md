# Late-card worker combines physical probes with injected authority

**Historical finding; current closure not revalidated.**

Physical occupancy/link probes and independent framed hash are meaningful when the worker runs; inspection denial is injected and external-authenticated allocation uses supplied digest strings. Scaffold overwrites registry files under caller root, promotion calls approved=true and no local disposable-root gate exists. completed aggregates booleans but does not set nonzero exit on false.

- [[Evidence/S1420]]
