# Archive inspectors have format and resource limits

**Historical finding; current closure not revalidated.**

Wheel reads every file payload and sdist materializes member list then reads payloads with no aggregate/member/decompression cap. Duplicate names are detected, but case/normalized aliases are not. Tar links rejected; ZIP Unix link mode is not checked. Other nonfile tar entries are ignored.

- [[Evidence/S2884]]
