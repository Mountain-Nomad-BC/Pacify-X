# Assembler source and receipt identities use separate reads

**Historical finding; current closure not revalidated.**

Input receipts parsed before per-record receipt hashing; same file can be reread and hashed once for each control. Concurrent edits may bind different bytes, and large receipts amplify read cost. Source manifest is not rechecked after merge. Input root containment occurs after input load.

- [[Evidence/S3369]]
