# Publisher terminal paths have different process closure guarantees

**Historical finding; current closure not revalidated.**

Timeout awaits termination result; output-limit and dispose request termination without awaiting. Stdin error calls finish, clears timer and active child reference without terminating it. Throwing onHealth can prevent promise resolution; per-stream output cap follows string concatenation.

- [[Evidence/S902]]
