# Exact harness tests mostly assert its own aggregate projections

**Historical finding; current closure not revalidated.**

Tests run fixtures then assert counts, inferred coverage fields, local effect lists and normalized repeat. Timeout test is one direct sleeping child; cache test observes simple corruption rejection and may reuse process cache. No deletion/outside effect, resealed cache, alternative interpreter or hard aggregate deadline coverage is established.

- [[Evidence/S4201]]
