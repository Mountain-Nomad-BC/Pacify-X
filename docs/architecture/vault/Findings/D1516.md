# Whole cache directory moves can carry uninventoried descendants

**Historical finding; current closure not revalidated.**

The walker can skip links and excluded subtrees while still selecting their containing cache directory. shutil.move then relocates the entire selected directory, including entries not in covered_files. Post-move verification checks only recorded inventory, so it is not a complete moved-object denominator.

- [[Evidence/S3668]]
