# Preflight ignores the reclamation result when scoring readiness

**Historical finding; current closure not revalidated.**

Owned workspace is ended and reclaim called in finally; returned cleanup receipt is not added to checks. A nonthrowing rejected/failed reclaim can coexist with passing checks and subsequent cache publication.

- [[Evidence/S2947]]
