# External disposition digest does not bind its input artifacts

**Historical finding; current closure not revalidated.**

The digest covers produced cards but omits index and catalog file digests. Report can remain unchanged across different unreferenced catalog/index content. Content-addressed output uses exclusive creation and CLI returns zero even when complete=false and cards are blocked.

- [[Evidence/S3769]]
