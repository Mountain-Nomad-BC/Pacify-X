# Audit verifier establishes internal consistency only

**Historical finding; current closure not revalidated.**

Checks outer hash, duplicate/unsafe names, declared per-file hashes and exact member set/count. It does not validate full manifest schema, payload byte totals, unique manifest rows, exclusion semantics or nonempty payload. Recomputed unkeyed hashes do not authenticate origin.

- [[Evidence/S2605]]
