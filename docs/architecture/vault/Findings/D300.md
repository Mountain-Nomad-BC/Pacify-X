# Portable profile validation checks keys rather than routing values

**Historical finding; current closure not revalidated.**

validate_profile requires local_models/cloud_models/sensitive_data keys without validating their values. Set validation collects IDs into a set and rejects missing known IDs but permits duplicates. Heavy-lane1 is a checked declaration, not runtime serialization; machine-path check is only selected substrings and reads file separately from parsed payload.

- [[Evidence/S2629]]
- [[Evidence/S2630]]
- [[Evidence/S4311]]
