# Test profile partition can tolerate a missing isolated file

**Historical finding; current closure not revalidated.**

Only top-level test files are discovered. Named isolated lists are filtered by existence and the profile fails only if empty; removal of one member can reduce coverage. Unit concurrency is four, UI lanes are serial, and child executions have no explicit timeout or resource registration in this runner.

- [[Evidence/S786]]
