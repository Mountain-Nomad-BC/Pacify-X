# Sidebar schema checks occur after full serialization

**Historical finding; current closure not revalidated.**

256 KiB message cap is applied after JSON.stringify allocation. Undefined serialization result reaches Buffer.byteLength outside serialization catch. Strict schema prevents unknown fields but arbitrary outbound input may contain eighty tasks per wave across twelve waves without a total-eighty refinement.

- [[Evidence/S1214]]
