# Local package hash does not authenticate requested extension identity

**Historical finding; current closure not revalidated.**

Source realpath, nonempty regular file and SHA-256 are checked, but VSIX package metadata is not read to bind requested extension ID/version. Whole file read has no size ceiling and native host opens path later. Native host owns its own package trust checks.

- [[Evidence/S1135]]
