# Durable goal blocker repetition cannot accumulate from refused transitions

**Historical finding; current closure not revalidated.**

block requires three consecutive matching history entries, but failed first/second block attempts are not appended because history is updated only when event_applied. Normal returned-state reuse therefore cannot reach blocked without preexisting caller-injected history. Block and complete do not require an in-progress source; supplied acceptance truthiness/nonempty evidence can complete planned or already complete state. No state hash or history integrity is verified.

- [[Evidence/S2045]]
