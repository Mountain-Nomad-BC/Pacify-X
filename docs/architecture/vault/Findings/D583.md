# Generated test index reuses and trusts stored metadata

**Historical finding; current closure not revalidated.**

Equal source sha and imports-list shape allow prior imports reuse without recomputing AST. Resolver checks config topology and exact test membership but trusts stored base_inputs and other group fields; stored unpack can override earlier valid. Non-structural newly matching inputs are not rediscovered until rebuild.

- [[Evidence/S3199]]
- [[Evidence/S3212]]
