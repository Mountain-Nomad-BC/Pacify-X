# Byte projection fixed denominators do not inspect all extras

**Historical finding; current closure not revalidated.**

Wrapper reconciler checks seven hardcoded destinations; suite template reconciler checks 3 by 7 paths. Neither discovers unexpected additional copies nor validates Python/JSON semantics. Each records pre-write stale list and returns true in apply mode after sequential normal writes.

- [[Evidence/S3480]]
- [[Evidence/S3477]]
