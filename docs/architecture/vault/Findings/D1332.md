# Map digest refresh does not restore source evidence validity

**Historical finding; current closure not revalidated.**

Rehash updates inventory_sha256 from current map bytes and replaces an existing Markdown hash line. It does not validate records, source hashes, counts or the claimed preceding sanitization. Multiple partitions and JSON/Markdown writes can partially finish.

- [[Evidence/S3732]]
