# Nonprimary card states can abort reconciliation planning

**Historical finding; current closure not revalidated.**

The planner skips selected terminal, host-owned and excluded cards, then calls CARD_PRIMARY_STATES.index(state). A bound eligible card in blocked, deferred or reopened state can raise rather than produce a controlled nontransition result.

- [[Evidence/S3921]]
