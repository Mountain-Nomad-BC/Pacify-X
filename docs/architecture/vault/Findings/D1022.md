# Import cycle traversal can revisit many paths

**Historical finding; current closure not revalidated.**

Cycle detection recursively explores paths from every node with active-set detection but no global visited/SCC reduction. Dense acyclic or cyclic graphs can cause large repeated work and recursion depth; no explicit time/node output budget bounds this stage.

- [[Evidence/S3067]]
