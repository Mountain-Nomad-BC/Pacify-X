# Provider source scan does not establish universal mediation

**Historical finding; current closure not revalidated.**

Scan corpus is runtime/scripts Python; gateway itself is excluded and detector uses selected SDK imports/call suffixes. Cached records reuse prior violations for unchanged source bytes without a scanner-revision check. Current index validates source identity but does not independently recompute its findings. This cannot prove host JavaScript or arbitrary adapter/network calls are mediated.

- [[Evidence/S2720]]
- [[Evidence/S4318]]
