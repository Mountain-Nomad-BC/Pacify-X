# Installed audit requires one changed but equivalent package JSON

**Historical finding; current closure not revalidated.**

Byte-identical package.json increments identical rather than normalized; valid requires normalized == 1. A fully byte-identical install can fail. Only observed __metadata is removed for normalized equality.

- [[Evidence/S4004]]
