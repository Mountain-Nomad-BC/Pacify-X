# Worker diagnostic fallback tests intercept storage and stderr

**Historical finding; current closure not revalidated.**

Primary diagnostic failure is injected before a second durable write. When both writes fail, os.write is intercepted and the JSON payload inspected. No real supervised stderr transport, short-write loop or total diagnostic failure is exercised by these tests.

- [[Evidence/S4087]]
