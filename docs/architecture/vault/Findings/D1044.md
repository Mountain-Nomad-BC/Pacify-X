# Doctor report retention does not validate incoming report seal

**Historical finding; current closure not revalidated.**

Retention accepts report_sha256 string of length64, without hex syntax, recomputation or full report schema verification. It embeds supplied report and seals new receipt; a valid receipt can preserve a caller-supplied inconsistent report. Path/transaction safety remains delegated to JSON WAL.

- [[Evidence/S2755]]
