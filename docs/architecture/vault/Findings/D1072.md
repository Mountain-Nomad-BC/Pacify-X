# Installed test can build unless certified wheel variable is supplied

**Historical finding; current closure not revalidated.**

Certified-wheel branch checks supplied hash and no-build flag. If wheel variable absent, test builds regardless of no-build flag. Test-local install uses --no-deps without explicit --no-index and inherits environment; higher owner may supply offline configuration.

- [[Evidence/S4228]]
