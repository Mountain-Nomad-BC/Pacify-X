# Count inventory check is canonical text equality

**Historical finding; current closure not revalidated.**

CLI renders static table and compares read_text, which normalizes newline differences. It reports valid true after successful write/check; actual count values are validated only by runtime envelope consumer, not this script.

- [[Evidence/S3563]]
