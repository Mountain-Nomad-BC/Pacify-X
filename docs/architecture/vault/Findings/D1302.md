# Release timing is checked without proving admission event authenticity

**Historical finding; current closure not revalidated.**

Close requires exact release and automation stage insertion order, passing labels, sequential UTC windows and automation windows enclosing release windows. Admission/gap IDs are checked only as nonempty strings, not against ledger work admissions. Source digests bind the release kernel but are not recomputed from current repository bytes here.

- [[Evidence/S3846]]
