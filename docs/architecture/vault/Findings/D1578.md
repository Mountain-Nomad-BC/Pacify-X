# All workflow fixture routes use supplied preflight and evidence authority

**Historical finding; current closure not revalidated.**

The 17-workflow loop derives effect grants from the same handler registry and supplies all-true preflight, approved for non-read effects. Cross-project resolver returns fixed accepted source/destination evidence for every reference; many payloads provide true test/rollback flags and evidence strings. These exercise real handler routing when run, not independent trusted evidence acquisition.

- [[Evidence/S4413]]
