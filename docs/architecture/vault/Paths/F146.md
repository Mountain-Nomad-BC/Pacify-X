# Fault dimension declaration command execution and receipt

Runner checks declared union, runs lanes serially, hashes captured output and selected test files afterward, then writes summary.

1. [[Systems/tests]]
2. [[Systems/faultcampaignrunner]]

Narrative sequence is not automatically a proven call chain.

Timeout prevents partial receipt; no code-level proof ties declared dimensions to executed assertions.
