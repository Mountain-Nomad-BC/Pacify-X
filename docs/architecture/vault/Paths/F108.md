# Process record into catalog-dependent inert candidate

CLI reads supplied record; compiler checks schema/verification flags, ranks catalog ID/tag overlap and builds a sequential execution chain. Apply writes receipt then updates project evidence paths.

1. [[Systems/cli]]
2. [[Systems/processreceipt]]
3. [[Systems/processcompileidentity]]
4. [[Systems/skillstudio]]

Narrative sequence is not automatically a proven call chain.

No receipt authentication for verification claims, no recovered trace order, no automatic Studio package construction.
