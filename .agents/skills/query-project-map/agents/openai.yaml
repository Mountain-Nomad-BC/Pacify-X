interface:
  display_name: "Query Project Map"
  short_description: "Retrieve structural project evidence first"
  default_prompt: "Use $query-project-map to retrieve map evidence and a bounded source hydration plan."
