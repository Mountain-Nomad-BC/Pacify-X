---
name: px-runtime-environment
description: Query PX for startup, runtime, process, hardware, container, environment, tool, MCP, and resource-lifecycle capabilities. Use for execution-environment and performance work.
---

# Route runtime and environment work

Run `python -m runtime.cli --root . skill-query --goal "runtime startup process environment tools hardware containers MCP <task>"`. Select one admitted candidate and hydrate exactly one body.

Discovery is metadata-only; installation, service starts, and resource reclamation require separate authority.
