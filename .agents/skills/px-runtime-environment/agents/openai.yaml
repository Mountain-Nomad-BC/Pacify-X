interface:
  display_name: "PX runtime environment"
  short_description: "Query PX for px runtime environment capabilities"
  default_prompt: "Use $px-runtime-environment to query PX and hydrate one eligible capability."
