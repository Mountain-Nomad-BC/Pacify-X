interface:
  display_name: "PX debug repair"
  short_description: "Query PX for px debug repair capabilities"
  default_prompt: "Use $px-debug-repair to query PX and hydrate one eligible capability."
