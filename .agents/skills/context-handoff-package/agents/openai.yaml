interface:
  display_name: "Context Handoff Package"
  short_description: "Produce a compact, evidence-linked state transfer that lets another agent contin"
  default_prompt: "Use $context-handoff-package to produce a compact, evidence-linked state transfer that lets another agent continue without reconstructing the project."
