interface:
  display_name: "PX query skills"
  short_description: "Query PX for px query skills capabilities"
  default_prompt: "Use $px-query-skills to query PX and hydrate one eligible capability."
