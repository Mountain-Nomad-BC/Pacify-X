interface:
  display_name: "Audit Interactive Systems"
  short_description: "Audit interactive behavior and recovery paths"
  default_prompt: "Use $audit-interactive-systems to audit interactive behavior, failure paths, and recovery with bounded evidence."
