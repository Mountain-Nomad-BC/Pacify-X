orchestration_id: project_switch
title: Safely switch an agent session between projects
version: 0.1.0
status: design-ready
trigger: explicit-or-policy-scheduled
project_binding: required unless workspace bootstrap
skills:
- checkpoint-resume-manager
- project-session-router
- semantic-context-firewall
- agent-lease-manager
required_context:
- workspace_id
- agent_id
- session_id
- intent_id
- correlation_id
preflight:
- constitution_resolved
- lease_valid
- scope_resolved
- side_effect_budget_set
outcomes:
- new_project_session_active
failure_policy: checkpoint-and-fail-closed-on-boundary-or-integrity-violation
audit: true
