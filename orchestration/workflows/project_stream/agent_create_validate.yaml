orchestration_id: agent_create_validate
title: Create, sandbox, test, and register an agent
version: 0.1.0
status: design-ready
trigger: explicit-or-policy-scheduled
project_binding: required unless workspace bootstrap
skills:
- agent-factory
- agent-template-catalog
- agent-identity-permissions
- agent-registry-lifecycle
- agent-performance-health
required_context:
- workspace_id
- agent_id
- session_id
- intent_id
- correlation_id
preflight:
- constitution_resolved
- lease_valid
- scope_resolved
- side_effect_budget_set
outcomes:
- agent_active_or_rejected
failure_policy: checkpoint-and-fail-closed-on-boundary-or-integrity-violation
audit: true
