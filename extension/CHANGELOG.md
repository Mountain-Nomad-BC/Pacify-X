# Changelog

## 0.6.52

- Make extension bridge tests install and exercise the exact locked Python runtime on every CI platform.
- Canonicalize physical paths across Windows short names and macOS temporary-directory aliases.
- Preserve deep-path, Studio revision, and cross-platform process-tree verification without weakening their controls.

## 0.6.51

- Keep the owned-host timeout referenced until its termination result settles so Node 22 cannot cancel lifecycle verification early.
- Rebuild portable generated manifests from Git-normalized source bytes for clean-checkout parity.

## 0.6.50

- Integrate the current dependency and GitHub Actions updates without rewriting the issued 0.6.49 package.
- Refresh governed generated evidence and compatibility checks for the updated Python release toolchain.
- Keep extension packaging reproducible under the issued-artifact immutability guard.

## 0.6.49

- Restore exact editable Agent model-version handling and preserve binding, grant, and test validation counts through live validation.
- Route the composed dashboard snapshot through the governed runtime work plane while retaining runnable Agent and Workflow revision/run counts.
- Expose honest host-startup attribution and exact milestones, remove hidden request-only bindings from the editable-control denominator, and cover all 941 current controls without probe errors.
- Make the resource-heavy Studio/Memory/Graph governed gate deterministic on Windows by preventing competing supervised child-process chunks.

## 0.6.48

- Allocate newly added optional Agent graph nodes to an unoccupied canvas position so every node remains physically selectable and editable.
- Exercise conditional Agent, Workflow, and Skill Studio controls through exact disposable prerequisites in current-source and installed-host evidence walks.
- Run exhaustive and fault/recovery control evidence through bounded parallel browser pages while preserving isolated state and exact control ordering.

## 0.6.47

- Restore exact Agent and Workflow Start controls after their eligible preview or dry-run, bind submitted runs to their originating request, and accept the runtime-owned asynchronous start receipt.
- Validate and supply required Workflow run inputs before dispatch so a newly created workflow can start, reach a terminal success state, and reopen from durable history.
- Continue exact Skill candidates from the native catalog through validation, admission, and promotion, while retaining normal-session host confirmations.

## 0.6.46

- Prove immutable Agent, Workflow, and Skill candidate creation through their real installed-host Studio controls, including exact typed durable receipts and catalog persistence after reopening each route.
- Keep normal user confirmation boundaries while allowing only the double-confirmed owned isolated host to authorize reversible candidate-save evidence.
- Select the editable core scope deterministically before exercising Agent or Workflow Studio, preventing a retained Environment or Enterprise view from hiding the authoring control.

## 0.6.32

- Keep canonical Memory attached while the Pacify-X host remains active by renewing a sliding idle-expiry lease instead of exhausting a cumulative eight-hour session lifetime.
- Raise the configurable per-renewal lease ceiling from eight hours to 24 hours while preserving automatic expiry when the host stops renewing.

## 0.6.31

- Recover Studio setup from an occupied immutable Agent revision whose builder artifacts are invalid by allocating and creating the next fresh version.
- Bind installed-host verification to the exact Agent and Workflow versions returned by setup, allowing their immutable histories to advance independently.

## 0.6.30

- Restore durable candidate lifecycle continuation after reload for exact Agent and Workflow Studio revisions.
- Accept and validate the runtime-owned canonical creation timestamp in Agent Studio commit receipts.
- Replace arbitrary Agent preflight test text with eight explained runtime-owned selectors, unknown-ID rejection, and inspectable per-check outcomes.
- Bind Agent Studio to an exact local Ollama route and retain the real objective, model, output contract, and durable run lifecycle around host execution.

## 0.6.29

- Publish Agent and Workflow Studio starters as immutable bundle revision 1.0.1 so Setup Studio preserves older 1.0.0 definitions instead of colliding with them.
- Exercise real-boundary setup twice and require installed-host catalog selection to match the exact starter bundle version.

## 0.6.28

- Opens Agent, Workflow, and Skill Studio navigation directly in their editable authoring workspaces.
- Reopens hash-bound Agent and Workflow candidates without incorrectly requiring runtime admission merely to edit them; execution still requires authenticated admission.
- Allows external Agent and Workflow catalog definitions to open as independent editable Studio candidates without claiming predecessor lineage or inherited authority.
- Extends installed-VSIX certification through real Agent, Workflow, and Skill create/save/admit/run/reopen boundaries.

## 0.6.27

- Studio navigation and deep links now open the editable Agent, Workflow, or Skill authoring workspace immediately instead of stopping at a catalog surface.
- Hash-bound physical Agent and Workflow candidates can be reopened for a new revision before lifecycle admission; run authority still requires an authenticated admitted revision.
- Governed dashboard verification now includes real host-to-Python Agent, Workflow, and Skill creation, persistence, catalog, reopen, and bounded run round trips.

## 0.6.26

- Synchronize the exact current-source UI control denominator and retain typed current-host observations for every attempted control.
- Refresh the release toolchain to vulnerability-free, hash-pinned Python dependencies.
- Strengthen operational-ledger replay compatibility without weakening validation of newly appended discoveries.

## 0.6.25

- Expose the same bounded Studio setup flow as a normal VS Code command and confirmation-gated extension URI so the installed host uses its real SecretStorage signing identity.

## 0.6.24

- Add a one-confirmation setup path that creates or safely reuses editable starter Agent and Workflow revisions, registers bounded local authority, admits both revisions, and retains successful local run receipts.
- Fix Agent Studio's runnable counter to consume the canonical `agents_runnable_revisions` snapshot field.
- Replace the zero-runnable dead end with an actionable setup control across Agents, Agent Studio, and Workflow Studio.

## 0.6.23

- Restore the intended UTF-8 symbols in the dashboard controller instead of rendering double-decoded mojibake.
- Bind every live dashboard navigation surface into the generated typed operational inventory and keep the scaffold expectations aligned with the four Studio surfaces.

## 0.6.22

- Emit a version-distinct recovery artifact from the current integrated control-plane source so VS Code cannot reuse the older 0.6.21 host bytes.

## 0.6.21

- Register the Agent topology edit and request-bound version-suggestion controls in the authoritative generated UI action contract so installed-artifact operational walks fail closed only on real runtime gaps.

## 0.6.20

- Ship the current Studio host coordinator, predecessor allocation proofs, exact source-selection trust, correlated detach/outcome delivery, and immutable create recovery paths.
- Use length-framed skill tree commitments, bounded exact-tree admission, fatal byte-preserving UTF-8 handling, portable path collision checks, and in-place failure retention.
- Parse and recompute the complete layered learning-promotion hash chain before projecting knowledge and memory state.

## 0.6.19

- Reset every surface to its top boundary during navigation so retained scroll cannot hide a page header or its primary controls.
- Report the deterministic CPU fallback as active when the optional TurboVec accelerator is absent or not admitted.

## 0.6.18

- Repair the webview style policy that blocked graph geometry, workflow placement, progress meters, and every other dynamic visual transform in the installed host.
- Add exact admitted-revision operation/reload and exact durable-run resume binding instead of silently creating or substituting revisions.
- Make Studio catalog/run reads non-initializing, sign Agent preflight receipts, and verify current Skill manifest, payload, promotion, and rollback state.
- Refuse unattested host tools and require terminal, unique tool evidence before a completed host run can succeed.

## 0.6.17

- Replace raw Studio receipt inference with authenticated lifecycle projection and exact revision binding.
- Repair host-model start, resume, cancellation, token, tool-policy, and completion truth while retaining VS Code host authority.
- Preserve exact model identity and workflow input/adapter contracts through the visual builders and runtime.
- Repair canonical-memory lease attachment, project-map freshness, skill-domain isolation, sidebar health truth, and full operational drilldowns.

## 0.6.16

- Center the knowledge graph on its selected source record at readable zoom and keep SVG/node geometry in the same deterministic order.
- Keep the Agent Studio identity strip compact in narrow hosts and preserve a valid raw numeric max-output-token value.

## 0.6.15

- Correct the root `/control-plane` route so a normal dashboard launch no longer opens an unknown-entity modal.
- Add the installed-host operational UI walker used to traverse every exposed surface and the contributed sidebar in a real VS Code process.

## 0.6.14

- Replaced decorative Agent and Workflow Studio strips with selectable execution topology, typed ports, direct connections, bounded layout controls, exact execution previews, and durable run controls.
- Added admitted VS Code language-model and tool execution while preserving host authority and fail-closed PX bindings.
- Rebuilt the knowledge graph as a progressive record map with source coverage, communities, provenance, search, analysis, pan/zoom, minimap, and an accessible list equivalent.
- Connected the evidence-to-pattern-to-hypothesis-to-trial-to-promotion/decay learning pipeline to authenticated production operations.
- Corrected sidebar subtask routing, stale activity presentation, provider and memory health truth, Studio custody, and request-order handling.

## 0.6.13

- Correlate asynchronous failures with their active request so stale graph, catalog, memory, and activity failures cannot overwrite newer results.
- Keep Studio drafts open through host approval and preserve them unchanged when approval or source selection is canceled.
- Repair MCP, canonical-memory, enterprise-readiness, and active-session truth labels against their actual live fields and freshness boundaries.
- Synchronize Skill Studio identity/version across the candidate, capability manifest, skill manifest, and SKILL.md frontmatter.
- Block invalid workflow edges at interaction time with a specific type, driver, duplicate, self-edge, or cycle reason.

## 0.6.12

- Persist validated Agent and Workflow Studio authority definitions with immutable revisions while keeping runtime input values ephemeral.
- Make workflow nodes, typed ports, exact port-to-port edges, edge removal, and node configuration genuinely editable and preserved by the Python owner.
- Require explicit per-run workflow inputs and retain truthful runtime/authority boundaries in reopened Studio revisions.
- Separate malformed sidebar messages from callback/outbound failures and record sanitized sidebar diagnostics in the Pacify-X output channel.

## 0.6.11

- Replaced generic graph blobs with semantic project/domain groups and added path, impact, dependency, dependent, hub, orphan, and provenance analyses.
- Made Agent Studio topology interactive, restored project Studio revisions to lazy catalogs, and blocked unsafe reconstruction of missing authority definitions.
- Repaired sidebar entity routing, stale activity classification, project-map visibility, truthful health labels, and top-bar control spacing.
- Removed non-functional blanket Inspect controls and exposed the actual learning-pipeline implementation boundary.

## 0.6.10

- Distinguish deliberately unconfigured provider telemetry from a failed or unavailable configured provider.
- Keep initial canonical-memory setup and ordinary Git dirtiness out of fault attention while retaining precise invalid-workspace and expired-lease warnings.

## 0.6.9

- Publish workflow terminal states only after the detached worker PID is proven absent and resource custody is closed.
- Repair 200%-equivalent responsive dashboard metrics and identity-banner separation with browser geometry assertions.
- Certify approved environment persistence and extension-listener registration in the exact installed host.
- Require audit-export completion projection identity and identical platform-independent VSIX bytes.

## 0.6.8

- Sanitize the retained exact-installed-host receipt so the connected engine root remains explicit without leaking a machine-specific absolute locator into portable evidence.

## 0.6.7

- Project canonical node coordinates through the live pan/zoom transform and canvas bounds for installed-host graph visibility evidence, avoiding Electron's zero SVG `foreignObject` rectangles.

## 0.6.6

- Measure installed-webview graph visibility from rendered SVG frames, with a bounded HTML-node fallback, so exact-host geometry evidence remains correct in Electron.

## 0.6.5

- Bind dashboard health to exact host/source asset identity and fail closed on version or protocol splits.
- Add complete count-preserving graph overview with cluster drill-down and non-overlapping responsive layouts.
- Connect Knowledge Core to its governed proposal lifecycle and separate live activity from retained history.
- Classify sidebar schema, type, and asset-protocol mismatches and reconcile stale operations append-only.

## 0.6.4

- Added durable, authenticated pause/cancel/resume and crash recovery for Agent and Workflow Studio runs.
- Added governed Knowledge Core proposal, verification, promotion, rollback, and recovery controls.
- Replaced raw Studio JSON-only editing with typed workflow and bounded skill-package editors.
- Added fuzzy graph filters/saved views, consolidated CSS debt, and exact 101-control action coverage.
- Added Edge/Chrome browser lanes and Linux installed-VSIX host certification support.

## 0.6.3

- Added complete authority-gated Agent, Workflow, and Skill Studio lifecycle operations with authenticated receipts and recoverable promotion transactions.
- Added bounded canonical-memory pagination and filters, truthful agent/workflow denominators, and safer Knowledge Core source custody.
- Hardened installed-host, cross-platform CI, provenance, dependency, message-contract, and generated-authority checks for the adversarial re-audit.
- Updated the exact MCP/extension package, runtime efficiency controls, and dependency pins without replacing previously issued VSIX bytes.

## 0.6.1

- Added the governed dashboard/sidebar bridge, Studio, memory, graph, activity, and environment surfaces.
- Added exact VSIX packaging, installed-host smoke evidence, bounded process custody, and hash verification.
- Added PX-native skill routing and separated standard from MS+Enterprise catalogs.
- Hardened message validation, workspace boundaries, cleanup controls, and runtime diagnostics.

## 0.5.5

- Negotiates sidebar render acknowledgements so a newly loaded webview remains compatible with an older in-memory host during the required reload boundary.
- Adds content-free sidebar contract-rejection diagnostics and prohibits replacing materially different VSIX bytes under an already-issued extension version.

## 0.5.4

- Repairs live Knowledge Graph deep linking and adds bounded request failure reporting plus host-observed render geometry.
- Re-spaces dashboard header controls with pairwise collision checks across compact and wide viewports.

## 0.5.3

- Adds live sidebar-provider resolution telemetry so host certification proves `resolveWebviewView` ran and assigned HTML.
- Makes exact installed-VSIX certification fail if the Control Center remains a tree placeholder or never resolves its webview.

## 0.5.2

- Prevents workspace-less windows from passing an undefined root into the MCP environment-path resolver.
- Documents and certifies the full-application restart boundary required when migrating a contributed view from tree data to webview data.

## 0.5.1

- Repairs the first-open Control Center by declaring it as the webview implemented by the extension instead of allowing VS Code to construct a tree view.
- Ships the transactional, stable-API activation path and bounded startup modules that were absent from the obsolete installed `0.5.0` artifact.
- Makes exact installed-VSIX smoke tests derive the artifact name from the package manifest so repair versions cannot silently test stale bytes.

## 0.5.0

- Adds a project-owned, hash-linked, metadata-only activity ledger with correlation, task/claim attribution, actor presence, active-operation state, bounded search, and explicit privacy limitations.
- Wires editor, filesystem, terminal, task, test, debug, SCM, extension, PX-launched Codex, and MCP lifecycle observations into a dedicated Activity dashboard.
- Adds `pacify_activity_emit` and `pacify_activity_observability`; all 35 MCP tools now produce start/success/failure trace metadata without capturing tool payload contents.
- Adds pause/resume policy controls, human-readable and machine-readable event inspection, JSON export, responsive/touch-friendly activity views, and regression coverage.

## 0.4.4

- Rebuilds the Knowledge Graph as a navigable workspace with flow/orbit layouts, drag and trackpad panning, touch pinch zoom, Ctrl+wheel zoom, keyboard controls, fit/reset actions, a minimap, and collapsible connection details.
- Adds node/path isolation, directional connection badges, selected-node context, larger touch targets, resize handling, reduced-motion behavior, forced-color support, and a browser-driven interaction regression.

## 0.4.3

- Separates canonical vault telemetry from non-canonical project coordination memory.
- Adds sealed portable-memory observability, bounded search, readable/JSON record inspection, and an AI-facing read-only MCP tool.
- Expands canonical workspace memory monitoring with lifecycle, layer, integrity, index, and write-queue evidence.

## 0.4.2

- Added a conservative nine-dimension agent-readiness matrix with evidence, explicit gaps, a structural score cap, human-readable inspection, JSON export, and a read-only MCP tool.
- Added an interactive governed agent-model popup that explains identity, capability matching, safety boundaries, lifecycle, handoffs, verification readiness, and provenance.
- Strengthened the Plugin Manager with a typed catalog/admission contract for bounded human and AI discovery without treating catalog presence as installation or authority.
- Incorporated clean-room patterns from the final external intake while rejecting bulk activation of an unvetted 121k-item catalog and vulnerable-driver sensor code.

## 0.4.1

- Replaced the aggregate Knowledge Graph mock with bounded, searchable capability and repository graphs using deterministic nodes, typed directed edges, relationship explanations, filters, and truncation disclosure.
- Added human-readable information inspectors with an accessible machine-readable JSON tab, copy support, and exact-record JSON export.
- Repaired the control-rail logo by using the complete square shield asset with rail-specific contain sizing.
- Gave execution bindings stable names and summaries, including the formerly opaque workflow records 52 onward.
- Added read-only CPU/GPU thermal and utilization telemetry with source labels, bounded probes, and explicit unavailable states.
- Added a governed plugin manager for extension, skill, tool, MCP, and connector inventory without granting discovery-time activation or install authority.
- Added MCP tools for capability manifests, graph queries, hardware telemetry, and plugin catalogs so AI clients receive the same structured information as the UI.
- Clean-room adopted bounded visualization, dual-format inspection, plugin-lifecycle, and sensor-adapter mechanisms after provenance and license review; no external code was copied.

## 0.4.0

- Replaced extension-side registry parsing with the versioned Pacify-X `runtime.dashboard_api` and complete lazy catalogs.
- Added a project-owned cross-IDE coordination ledger: task DAGs, ownership, dependency gates, file/area claims, leases, dispatch, receipts, conflict detection, reconciliation, and portable handoffs.
- Added layered rolling memory from session to project to verified state to review-only system candidates.
- Expanded the bundled stdio MCP surface from four read-only tools to twenty-seven context, catalog, coordination, task, memory, environment-map, guardrail-evaluation, worker-doctor, WorkRoom, team-package, and MS+Enterprise tools with correct effect annotations.
- Integrated the Team Fabric pack into the existing coordination owner with claim modes, authority states, fencing, budgets, adapter doctor records, stop diagnostics, derived rooms, and governed candidate imports.
- Added the executable data-sort dry-run picker, three-candidate benchmark orchestration, and a broad sorting-algorithm knowledge catalog.
- Added a separately namespaced MS+Enterprise control plane: 18 packs, 20 skills, 12 agents, 8 workflows, 10 connectors, 2 providers, project-local state/events/receipts, readiness doctor, and Skills/Agents/Workflows tabs.
- Added a default-off billable-provider policy switch and independently enforced cost, token, local-first, provider, hardware, confidence, cache/reuse, and human-approval guardrails. No provider executor or credential storage was added.
- Added startup/change/manual discovery of VS Code extensions, contribution points, commands, local tools, Python packages, and npm packages using a compact index, lazy hash-verified shards, per-extension contracts, and a semantic capability graph.
- Completed a 23-file enterprise-master audit and a 33-archive/112,905-entry cross-check, including explicit Agent Academy version drift and second-pass Power Automate, mobile/offline, session-isolation, approval/filtering, maturity/versioning, message-mode, and FinOps coverage.
- Added the `parallel-planning-coordination` skill and orchestration resources.
- Rebuilt every surface around the supplied Pacify-X branding with a persistent vertical rail, complete searchable/paged lists, detail modals, focus management, and reduced-motion handling.
- Hardened cleanup with scan, preflight, and immediate full-tree equality gates plus receipts for Recycle Bin and permanent disposition.
- Removed billable API fallback; optional Ollama remains local-only and disabled by default.
- Made package version, VSIX name, checksum, and installed-version verification derive from one source of truth.

## 0.3.1

- Introduced the cockpit theme, Control Center, portable Codex context, local Ollama candidate, and generated-cache cleanup.

## 0.2.0

- Built the initial ten-visible plus two-advanced dashboard shell.
